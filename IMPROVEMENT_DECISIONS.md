# 개인 업무 문서 RAG — 개선 결정과 경력서 활용

작성일: 2026-09-05. 이번 범위는 로드맵의 **첫 개선 묶음**이다. 전체 확장 기능을 구현했다는 뜻은 아니다.

## 1. 목표와 선택 이유

목표는 두 가지다. 실제 업무에서 기억나는 내용으로 문서를 찾고 원문 검증을 빨리 끝내는 것, 그리고 문제 정의·기술 선택·검증·운영 개선 과정을 설명할 수 있는 개인 프로젝트를 만드는 것이다.

기능 수를 늘리는 대신 검색 범위의 신뢰성, 실패 시 데이터 보존, 원문 확인 동선, 평가의 정직성을 먼저 개선했다. 결과를 잘못 믿거나 재색인 실패로 기존 검색까지 잃는다면 채팅 기능을 추가해도 업무 도구로 쓰기 어렵기 때문이다.

Ponytail의 단순성 원칙에 따라 기존 Python 표준 HTTP 서버와 SQLite를 유지하고, Unlazy의 검증 게이트에 따라 실패 주입 테스트와 실제 브라우저 확인을 수행했다. 새 프레임워크·벡터 서버·그래프 DB는 추가하지 않았다.

## 2. 무엇을 왜 바꿨는가

| 변경 | 해결하려는 문제 | 구현과 트레이드오프 |
|---|---|---|
| 현재 폴더로 검색·통계·출처 제한 | 같은 DB로 폴더를 바꿨을 때 이전 자료가 섞임 | 서비스의 source_root를 공통 조회에 적용. 이전 색인을 지우지는 않으며 조직 권한 기능도 아님 |
| 파일 해시를 파싱 전에 비교 | 변경 없는 문서까지 파싱하는 낭비 | 읽기·해시 비용은 남지만 파싱·임베딩을 생략 |
| 준비 후 파일별 원자적 교체 | 임베딩 또는 DB 저장 실패로 기존 근거 소실 | 파싱·임베딩 성공 후 SQLite 트랜잭션에서 교체. 전체 폴더 일괄 롤백이 아니라 파일별 보존 |
| 공유 DB 접근 직렬화 | HTTP 요청이 하나의 연결을 동시에 사용 | 단일 사용자 RLock. 중복 색인은 오류로 알리고 검색은 색인 완료를 기다릴 수 있음 |
| 검색·질문 공통 근거 뷰어 | 파일 경로만 있어 원문 확인 동선이 끊김 | 청크 전체·위치·색인 시점 표시, 원본 확인, 인용 복사 피드백. 비PDF는 다운로드 |
| 원본 변경 확인 | 색인 당시 인용과 수정된 원본 혼동 | 원본 해시 불일치 시 409와 재색인 안내. 뷰어는 과거 청크임을 명시 |
| 평가 지표 정의 수정 | 인용 번호 존재를 답변 정확도로 오해 | 문서 hit·인용 존재·정답 파일 일치를 분리하고 의미적 정확도는 null |
| 네 구성과 실행 메타데이터 | reranker 효과와 실행 환경을 분리하기 어려움 | vector, BM25, hybrid, hybrid+rerank 비교 및 코드/질의/문서 해시 기록 |
| 평가의 명시적 오프라인 실행 | 환경 API 키로 예상치 못한 호출·비용 발생 | 임베딩과 답변 모두 키를 비워 네트워크 없이 실행. 실제 LLM 품질의 증거는 아님 |

주요 코드: `rag/index.py`, `rag/service.py`, `app.py`, `web/index.html`, `scripts/run_eval.py`.

## 3. 사용 흐름

```powershell
python app.py --data data/sample --db .local/rag.sqlite3 --port 8000
```

1. 브라우저에서 `http://127.0.0.1:8000/`을 연다. 서버 실행 자체가 브라우저를 자동으로 열지는 않는다.
2. 화면에 표시된 검색 폴더를 확인하고 **변경 파일 색인**을 누른다. 해당 폴더와 하위 폴더의 PDF·DOCX·MD·TXT만 대상이다.
3. **검색**은 관련 문단 목록을 보여준다. **질문**은 검색된 근거를 바탕으로 답변하며, 키가 없으면 원문 발췌 방식이다.
4. 근거 위치 확인으로 색인 당시 청크와 위치를 확인한다. 필요하면 원본 파일을 확인한다.
5. 확인한 인용을 복사해 메모 등에 활용한다. 수정·추가·삭제한 문서는 다시 색인한다.

폴더를 옮겨 모을 필요 없이 기존 폴더를 지정할 수 있다. 현재는 하나의 루트만 지원하므로 서로 떨어진 여러 폴더 등록과 자동 감시는 후속 범위다. API 모드에서는 문서 텍스트가 외부 서비스로 전송될 수 있으므로 회사 환경의 허용 범위를 확인해야 한다. 공개 저장소에는 실제 회사 문서·경로·키·색인 DB를 넣지 않는다.

## 4. 검증과 해석

```powershell
python scripts/run_tests.py
python scripts/run_eval.py --data data/sample --questions data/eval/questions.jsonl --output results/evaluation.json
```

회귀 테스트는 폴더 분리, 임베딩 실패 시 이전 청크 보존, SQL 삽입 실패 시 문서·청크·FTS 롤백, 재시도, 미변경 파일 처리 생략, 중복 색인 거절, 오프라인 평가의 네트워크 미호출, HTTP 근거 escaping·원본 다운로드·변경/없는 출처 오류를 확인한다. API 관련 기존 테스트는 모의 응답이며 실제 유료 API 품질 검증은 아니다.

브라우저에서는 별도 포트 8001에서 예시 문서 20개 색인, RRF 검색, 근거 뷰어 이동, 인용 복사 성공 피드백, 질문 답변과 출처 카드 표시를 직접 확인했다. 클립보드 내용을 다시 읽어 비교한 것은 아니며, PDF 외부 뷰어의 동작까지 브라우저에서 검증한 것은 아니다.

첫 추가 HTTP 테스트는 Windows의 CRLF 변환 때문에 문자열 비교에 실패했다. 다운로드 요구사항에 맞게 디스크 원본 바이트와 응답 바이트를 비교하도록 수정했다. 파일 내용을 정규화해서 버그를 숨기지 않는다.

평가 조건은 합성 Markdown 문서 20개, 초안 질의 50개(답변 가능 47, 불가 3), 로컬 hash embedding, 추출형 답변이다. `document_hit_at_5`는 답변 가능 질의 중 상위 5개 결과에 지정 정답 문서가 한 번이라도 포함된 비율이다. `gold_document_citation_rate`는 실제 인용된 출처 중 정답 파일과 일치한 비율이다. `citation_presence_rate`는 답변 가능 질의에서 출처 인용이 있었던 비율이다. 어느 것도 주장 단위 의미적 정확도를 보장하지 않는다.

현재 결과에서 BM25와 hybrid+rerank의 문서 Hit@5가 같아, hybrid가 무조건 우수하다고 결론 내릴 수 없다. 수치는 `results/evaluation.json`을 기준으로 확인한다. 작은 합성 자료와 사람이 검증하지 않은 라벨에서 나온 결과를 회사 문서 성능이나 업무 시간 절감으로 일반화하지 않는다. `EVALUATION PASSED`는 평가 실행 완료이지 품질 목표 달성 판정이 아니다.

## 5. 경력서에 지금 쓸 수 있는 내용

분류는 **개인 프로젝트 / 프로토타입**이 적절하다. 회사에서 운영했다는 이력이나 OpenAI FDE 채용 적합성이 검증됐다는 표현은 아직 근거가 없다.

프로젝트명 예시: **근거 추적과 실패 복구를 지원하는 개인 업무 문서 검색·RAG**

> PDF·DOCX·Markdown·TXT의 본문과 위치 정보를 보존하는 개인 문서 검색·RAG 프로토타입 개발. BM25·벡터 검색·RRF·재정렬 비교와 근거 뷰어를 구현하고, 파일별 트랜잭션 및 실패 주입 테스트로 증분 색인의 데이터 보존을 검증.

> 합성 문서 20개·질의 50개에서 네 가지 검색 구성을 비교하는 오프라인 평가 파이프라인 구축. 문서 hit와 인용 존재·정답 파일 일치를 분리하고 코드·데이터 해시를 기록해 평가 재현성을 확보.

개발에 AI 도구를 활용한 사실과 본인이 직접 이해·검토·실험한 범위를 설명할 수 있어야 한다. 숫자는 측정한 것만 쓰고, 직접 하지 않은 고객 협업·실서비스 배포·사용자 수·비용 절감·정확도 향상은 적지 않는다.

## 6. 경력서의 설득력을 높이는 다음 실험

### A. 실제 업무 효율: 개인 파일 탐색 문제를 증명

대표 과제 10~15개를 고른다. 단순 파일 찾기, 특정 결정 근거 찾기, 서로 다른 문서 비교를 포함한다. 기존 방식과 도구 사용을 번갈아 수행해 순서 편향을 줄이고, 시작부터 **정확한 원문을 확인할 때까지** 시간을 잰다. 실패도 제외하지 않는다. 동일 과제를 반복하면 기억 효과가 있으므로 비슷한 난이도의 과제와 순서를 기록한다.

| 과제 ID | 방식 | 소요 초 | 원문 확인 성공 | 답변 수정 필요 | 실패 유형 |
|---|---|---|---|---|---|
| 직접 측정 후 입력 | 기존 / RAG | 실측 | 예 / 아니오 | 예 / 아니오 | 검색 누락 / 잘못된 근거 / 기타 |

개인 1인의 결과임을 명시하고 중앙값·성공률·표본 수를 함께 보고한다. 다른 사용자에게 일반화하지 않는다. 추후 문구는 “개인 업무 과제 N개에서 원문 확인 시간 중앙값 A초→B초, 성공률 C→D”처럼 실제 측정 후 작성한다.

### B. LLM 프로젝트로서 품질 검증을 강화

실패 사례를 바탕으로 약어·표현 변형·숫자·최신/구버전·답 없는 질문·문서 간 비교 질문을 늘리고, 정답 문서뿐 아니라 근거 문단과 답변 허용 범위를 사람이 검토한다. 튜닝용과 최종 평가용을 분리한다. 허용된 API 환경에서는 동일 질문으로 실제 임베딩·LLM 답변의 주장별 근거 일치, 부적절한 답변, 지연, 실제 usage 비용을 측정한다. 현재 오프라인 점수를 LLM 성능으로 대체하지 않는다.

### C. 측정된 병목에 따라 확장

- 여러 기존 폴더를 자주 바꿔야 하면 복수 루트 등록·제외 경로·중복 처리부터 추가한다.
- 수동 색인을 놓치는 일이 반복되면 변경 감시와 작업 상태·취소를 추가한다.
- 표·스캔 자료가 실제 검색 실패 원인이면 표 파싱·OCR을 추가한다.
- 문서 수 증가로 검색 시간이 병목이면 현재 전수 검색을 측정한 뒤 FTS/벡터 검색 저장소 활용을 개선한다.
- 관계 질문에서 일반 RAG의 실패가 확인될 때만 작은 엔티티/관계 추출부터 시작한다. 온톨로지·GraphRAG 자체를 경력서 장식으로 추가하지 않는다.

다중 사용자 권한, 조직 커넥터, 자동 문서 수정은 현재 개인 프로젝트 범위에 넣지 않았다. 다음 단계의 핵심 산출물은 기능 목록이 아니라 **실패 사례 → 변경 가설 → 비교 실험 → 측정된 결과**다.
