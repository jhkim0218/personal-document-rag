# Personal Work Document RAG — 신뢰성과 업무 효율 중심 설계서

## 1. 프로젝트 개요

### 한 줄 정의

로컬의 개인 업무 문서(PDF, Markdown, DOCX, TXT)를 파일명뿐 아니라 **본문 의미와 정확한 근거 문단**으로 찾고, 여러 문서를 바탕으로 질문에 답하는 개인용 지식 탐색 도구다.

### 해결하려는 문제

기존 로컬 파일 검색은 파일명·경로·정확한 키워드를 기억할 때만 잘 작동한다. 반면 사용자는 다음처럼 문서의 내용, 맥락, 여러 문서의 관계를 기준으로 찾고 싶다.

- “작년에 읽은 RAG 논문 중 reranking 실험이 있던 자료를 찾아줘.”
- “내 회의록에서 배포 일정이 언제로 합의됐지?”
- “내 노트와 논문을 함께 근거로 hybrid retrieval의 장단점을 설명해줘.”

이 프로젝트의 목표는 단순한 ‘문서와 대화하는 챗봇’이 아니다. **업무 문서를 다시 찾고 확인하는 시간을 줄이면서, 검색·인용·답변 거절의 신뢰성을 측정하고 개선하는 RAG 시스템**을 만드는 것이다.

### 제품 원칙

1. **찾은 뒤 바로 검증할 수 있어야 한다.** 요약보다 원문 위치와 발췌가 먼저다.
2. **업무 흐름을 하나라도 확실히 단축한다.** 답변이 멋져도 원문을 찾는 시간이 줄지 않으면 실패다.
3. **정확성을 과장하지 않는다.** 모르는 내용은 보류하고, 개선 수치는 같은 평가 조건에서만 말한다.
4. **개인용 도구답게 가볍게 유지한다.** 원본 파일을 바꾸지 않고, 변경된 파일만 다시 색인한다.

## 2. 범위와 전제

### 전제

- 색인하는 문서는 사용자가 소유하거나 처리 권한을 가진 문서로 한정한다.
- 첫 버전은 한 명의 개인 사용자를 위한 로컬 앱이다. 계정, 역할, 멀티테넌시, 조직 권한 관리는 범위에서 제외한다.
- 예시 데이터로 먼저 기능과 평가를 완성한 뒤, 사용자의 회사 PC에서 본인이 허용받아 처리할 수 있는 업무 문서를 개인적으로 색인한다.
- 공개 저장소와 포트폴리오에는 예시 문서·익명화된 집계 지표·재현 코드만 포함한다. 실제 업무 문서, 원문 발췌, 파일 경로, API 키는 포함하지 않는다.

### 포함 범위 (MVP)

- 지정 로컬 폴더의 PDF, Markdown, DOCX, TXT 색인
- 키워드·벡터 기반 hybrid 검색
- 질문에 대한 근거 기반 답변 및 파일/페이지/문단 인용
- 근거가 충분하지 않을 때의 답변 보류
- 변경 파일만 다시 처리하는 증분 색인과 색인 진행/실패 상태 표시
- 원문 열기와 인용 복사처럼 답을 실제 업무에 이어 쓰는 최소 기능
- 재현 가능한 평가 세트와 실험 결과

### 제외 범위

- 조직용 접근 제어, 사내 시스템 커넥터, 동기화 스케줄러
- 웹 크롤링, 자동 문서 작성·수정, 에이전트형 장기 작업
- 별도 지식 그래프 또는 무거운 온톨로지 구축

문서의 제목·작성일·형식·출처 같은 가벼운 메타데이터만 우선 사용한다. 명시적 개념 그래프가 검색 실패를 해결한다는 증거가 생길 때에만 후속 실험으로 검토한다.

## 3. 사용자 경험

### 3.0 핵심 업무 흐름

이 도구가 먼저 잘해야 하는 일은 세 가지뿐이다.

| 업무 흐름 | 기존의 불편 | 성공한 결과 |
| --- | --- | --- |
| 기억이 흐린 문서 찾기 | 파일명·폴더를 기억해야 함 | 자연어 질의로 관련 문단과 원문 위치를 찾음 |
| 회의·정책의 사실 확인 | 여러 파일을 열고 검색해야 함 | 짧은 답과 근거 문단을 함께 확인함 |
| 여러 자료 비교 | 노트·PDF를 오가며 수동으로 정리 | 문서별 근거가 붙은 비교 초안을 얻음 |

새 기능은 이 세 흐름 중 하나를 더 빠르거나 정확하게 만들 때만 추가한다.

### 3.1 검색 모드

사용자가 키워드 또는 자연어 질의를 입력하면 관련 문단을 반환한다.

- 문서 제목, 경로, 페이지 또는 헤딩, 원문 발췌를 표시한다.
- 정렬은 `hybrid retrieval → reranking` 순서로 수행한다.
- 결과를 클릭하면 원문 파일의 해당 위치를 열 수 있어야 한다.
- 검색 결과의 발췌만으로 관련성 판단이 가능해야 한다. 파일을 열기 전의 불필요한 왕복을 줄이기 위함이다.

### 3.2 질문 모드

사용자가 자연어로 질문하면 검색 결과만 근거로 답한다.

- 답변의 핵심 주장마다 인용 번호를 표시한다.
- 인용은 파일명, 경로, 페이지/헤딩, 원문 발췌로 추적 가능해야 한다.
- 검색 근거가 부족하거나 서로 충돌하면, 추측하지 않고 이유와 추가로 필요한 문서를 안내한다.
- 인용 묶음은 클립보드로 복사할 수 있어야 한다. 사용자가 회의록·메모·메신저에 근거를 다시 옮겨 쓸 수 있게 하기 위함이다.

### 3.3 대표 실패 처리

| 상황 | 기대 동작 |
| --- | --- |
| 관련 문서가 없음 | “색인된 문서에서 근거를 찾지 못했습니다”라고 답하고 유사 검색 결과만 제시 |
| 일부 근거만 있음 | 확인 가능한 사실과 불확실한 부분을 분리해 답변 |
| 문서끼리 충돌 | 각 문서의 주장과 날짜를 인용하고 단일 결론을 단정하지 않음 |
| 파일 파싱 실패 | 해당 파일·원인을 색인 결과에 기록하고 다른 문서 색인은 계속 진행 |

## 4. 성공 기준

MVP는 아래 기준을 모두 충족할 때 완료로 본다.

- 예시 문서 20~30개, 사람이 작성한 평가 질문 50개를 포함한다.
- 검색 모드 결과는 상위 5개 문단과 출처를 반환한다.
- 질문 모드의 모든 사실 주장에는 적어도 하나의 인용이 붙는다.
- 정답 문서가 있는 질문의 `Recall@5`, 인용 정확도, 근거 부족 질문의 적절한 답변 보류율을 측정한다.
- 기준선과 최소 두 가지 개선안을 동일한 평가 세트로 비교한다.
- README에서 실패 사례, 비용, 지연시간, 재현 방법을 공개한다.
- 실제 사용 단계에서 대표 업무 과제 10개 이상을 기존 파일 탐색과 이 도구로 각각 수행해, 과제 완료율과 원문 확인까지 걸린 시간을 기록한다.

수치 목표는 첫 기준선 측정 후 설정한다. 근거 없는 높은 목표를 먼저 정하지 않고, 개선 전후의 동일 조건 비교를 우선한다.

### 4.1 업무 효율 검증

RAG 지표가 좋아도 실제 업무에 편하지 않을 수 있다. 따라서 아래의 작은 사용성 검증을 별도로 한다.

1. 실제로 반복하는 업무 질문 10~15개를 고른다. 예: 특정 결정의 근거 찾기, 과거 자료의 정의 확인, 두 문서의 차이 요약.
2. 기존 방식(파일 탐색기·폴더·문서 내 찾기)과 도구 사용 방식으로 같은 질문을 해결한다.
3. 각 시도에서 시작부터 **원문을 열어 답을 확인할 때까지**의 시간, 성공/실패, 수정 필요 여부를 기록한다.
4. 시간이 더 걸리거나 근거가 부정확한 흐름은 기능 추가보다 먼저 고친다.

이 측정은 한 명의 사용자에 대한 사례 연구다. 이를 일반 사용자에게도 동일한 효과가 있다고 주장하지 않는다. 대신 “개인 업무 흐름에서 어떤 조건에서 시간을 절약했고, 어디서는 그렇지 않았는지”를 정직하게 보여준다.

## 5. 시스템 설계

```text
문서 폴더
  │
  ├─ 수집·파싱 ──→ 정규화·구조 추출 ──→ 청킹 + 메타데이터
  │                                           │
  │                               ┌───────────┴───────────┐
  │                               ↓                       ↓
  │                         키워드 인덱스            벡터 인덱스
  │                               └───────────┬───────────┘
  │                                           ↓
질문 ──→ query 처리 ──→ hybrid retrieval ──→ reranker ──→ 근거 문단
                                                               │
                                       검색 모드 ←─────────────┤
                                                               ↓
                                                    답변 생성 + 인용 검증
                                                               ↓
                                                    답변/거절/충돌 안내
```

### 5.1 수집 및 파싱

입력은 다음 형식으로 제한한다.

| 형식 | 추출 대상 | 필수 메타데이터 |
| --- | --- | --- |
| PDF | 페이지별 본문 | 경로, 파일명, 페이지 번호 |
| Markdown/TXT | 헤딩과 본문 | 경로, 파일명, 헤딩 |
| DOCX | 문단과 표의 텍스트 | 경로, 파일명, 문단 순서 |

각 청크에는 최소한 `chunk_id`, `document_id`, `path`, `title`, `location`, `text`, `indexed_at`, `content_hash`를 보관한다. `content_hash`로 수정되지 않은 파일의 불필요한 재색인을 피한다.

원본 문서와 인덱스 캐시는 분리한다. 색인 명령은 기본적으로 변경 파일만 처리하고, 사용자가 명시적으로 요청할 때만 전체 재색인을 수행한다. 앱 시작 시에는 마지막 색인 시각, 변경·추가·실패 파일 수를 보여준다.

### 5.2 청킹

기준선은 토큰 수 기반의 겹치는 고정 크기 청킹이다. 이후 헤딩·페이지·문단 경계를 존중하는 구조 기반 청킹과 비교한다.

- 청크에는 앞뒤 문맥을 일부 겹치게 넣는다.
- 표, 코드, 헤딩은 가능한 한 분리하지 않는다.
- 인용 위치를 잃지 않도록 원문 오프셋 또는 페이지/헤딩을 유지한다.

### 5.3 검색과 재정렬

1. 키워드 검색(BM25)과 벡터 검색으로 각각 후보를 얻는다.
2. 점수를 정규화해 합치거나 reciprocal-rank fusion으로 결합한다.
3. 상위 후보만 reranker로 재정렬한다.
4. 상위 근거 문단과 메타데이터를 답변 모델에 제공한다.

기준선은 벡터 검색 단독으로 둔다. hybrid retrieval과 reranker가 품질 향상에 기여하는지 평가로 검증한다.

### 5.4 답변 생성과 인용

답변 모델에는 검색된 문단만 컨텍스트로 제공한다. 출력은 아래 구조를 따른다.

```text
답변

근거
[1] 파일명 — 페이지/헤딩 — 뒷받침하는 원문 발췌
[2] 파일명 — 페이지/헤딩 — 뒷받침하는 원문 발췌

불확실성 또는 충돌 사항 (있을 때만)
```

프롬프트 규칙은 최소화한다.

- 제공된 근거에 없는 사실은 말하지 않는다.
- 인용 번호 없는 사실 주장은 생성하지 않는다.
- 충분한 근거가 없으면 답변을 보류한다.
- 서로 다른 근거가 충돌하면 충돌을 드러낸다.

생성 뒤에는 인용 번호가 실제 검색 청크에 존재하는지 형식 검증한다. 이 검증은 ‘인용이 답변을 실제로 뒷받침하는가’를 보장하지 않으므로, 별도 평가 항목으로 측정한다.

## 6. 평가 설계

### 6.1 평가 세트

평가 질문 50개를 직접 작성하고 다음 유형을 균형 있게 포함한다.

| 유형 | 예시 | 측정 목적 |
| --- | --- | --- |
| 정확 문구 검색 | “reciprocal rank fusion이 언급된 노트는?” | 키워드 검색 |
| 의미 검색 | “검색 결과 품질을 재정렬하는 방법은?” | 의미 검색 |
| 단일 문서 QA | “문서의 배포 일정은?” | 근거 충실성 |
| 다중 문서 종합 | “두 접근법의 차이는?” | 근거 결합 |
| 문서 부재 | “색인하지 않은 주제는?” | 적절한 답변 보류 |
| 문서 충돌 | “일정이 언제로 정해졌나?” | 충돌 노출 |

각 질문에는 정답 문서/청크, 기대 핵심 사실, 답변 가능 여부를 사람이 라벨링한다.

### 6.2 지표

| 지표 | 정의 |
| --- | --- |
| Recall@5 | 정답 청크가 검색 상위 5개에 포함된 질문의 비율 |
| Citation precision | 인용이 실제 답변의 핵심 주장을 뒷받침하는 비율 |
| Grounded answer rate | 근거 밖의 사실 없이 답한 비율 |
| Appropriate abstention | 답할 수 없는 질문에 올바르게 보류한 비율 |
| Latency | 검색과 전체 답변의 p50/p95 응답 시간 |
| Cost per query | 임베딩, reranking, 생성에 든 평균 비용 |

자동 지표만 믿지 않는다. 모든 실험에서 최소 10개 이상의 대표 사례를 사람이 검토하고, 인용 오류와 검색 오류를 구분해 기록한다.

### 6.3 실험 순서

| 실험 | 비교 | 의사결정 |
| --- | --- | --- |
| E1 | 벡터 단독 vs 키워드 단독 vs hybrid | 기본 검색 방식 선택 |
| E2 | 고정 청킹 vs 구조 기반 청킹 | 문서 전처리 방식 선택 |
| E3 | hybrid vs hybrid + reranker | 품질 향상 대비 지연시간/비용 판단 |
| E4 | 자유 답변 vs 인용 강제·답변 보류 | 신뢰성 UX 효과 판단 |

실험은 데이터셋, 모델, top-k, 프롬프트, 실행 시점을 기록해 재현 가능하게 한다. 한 번에 여러 변수를 바꾸지 않는다.

## 7. 구현 단계

### Phase 1 — 동작하는 기준선

- 예시 문서와 평가 질문 세트를 만든다.
- 파일 파싱, 고정 청킹, 벡터 검색 단독, 검색 UI를 구현한다.
- 질문 답변과 파일/페이지 인용을 구현한다.
- 기준선 지표를 측정한다.

완료 증거: 50개 질문에 대한 결과 JSON/CSV와 5개 이상의 실패 사례가 남아 있다.

### Phase 2 — 검색 품질 개선

- BM25를 추가해 hybrid retrieval을 구현한다.
- 구조 기반 청킹을 비교한다.
- 필요한 경우 reranker를 추가한다.
- E1~E3의 전후 결과를 표와 사례로 기록한다.

완료 증거: 기준선과 개선안이 동일 평가 세트에서 비교되어 있고, 채택한 방식의 이유가 수치와 사례로 설명된다.

### Phase 3 — 신뢰성 UX와 포트폴리오화

- 답변 보류·문서 충돌 처리와 인용 검증을 완성한다.
- E4를 수행하고 hallucination/인용 오류 사례를 분석한다.
- 데모 화면, 아키텍처 그림, 재현 안내가 포함된 README를 작성한다.

완료 증거: 신규 사용자가 예시 데이터로 색인·검색·평가를 재현할 수 있다.

### Phase 4 — 개인 업무 효율 검증

- 회사 PC에서 허용된 개인 업무 문서 폴더만 로컬 색인한다.
- 핵심 업무 흐름 10~15개에 대해 기존 방식과 도구 방식을 비교한다.
- 재색인 시간, 검색·답변 시간, 완료율, 실패 원인을 기록한다.
- 실제 문서와 식별 가능한 원문은 외부 저장소나 데모에 절대 포함하지 않고, 공개 가능한 범위의 집계 수치와 일반화한 실패 유형만 남긴다.

완료 증거: 날짜·과제 유형·측정 방법이 남은 사용성 결과표와, 유지할 기능/버릴 기능에 관한 결정 기록이 있다.

## 8. 기술 선택 원칙

- 초기에는 단일 로컬 프로세스와 로컬 파일 기반 저장을 선호한다.
- 이미 검증된 라이브러리로 PDF/DOCX 파싱, BM25, 벡터 인덱싱을 해결한다. 프레임워크를 여러 겹 쌓지 않는다.
- UI는 검색 결과, 인용, 실험 비교를 보여줄 수 있는 최소 웹 UI면 충분하다.
- 모델·임베딩 제공자는 교체 가능하게 하되, 처음부터 다중 제공자 추상화는 만들지 않는다.

모델 API를 사용한다면 API 키는 환경 변수 또는 로컬 비밀 저장소에서만 읽고, 저장소·예시 파일·실험 로그에 포함하지 않는다.

## 9. 저장소 산출물

```text
README.md                       # 문제, 아키텍처, 결과, 재현 방법
PERSONAL_DOCUMENT_RAG_DESIGN.md # 본 설계서
data/sample/                    # 공개 또는 직접 제작한 예시 문서
data/eval/questions.jsonl       # 질문, 정답 근거, 답변 가능 여부
src/                            # 수집, 색인, 검색, 답변 코드
results/                        # 실험별 지표와 오류 분석
docs/                           # 아키텍처 그림, 데모 이미지
docs/case-study.md              # 실제 사용성 측정 방법과 익명화된 결과
```

`data/sample/`에는 개인 문서나 회사 문서를 커밋하지 않는다. 사용자의 실제 문서 폴더 경로는 로컬 설정 파일 또는 환경 변수로만 지정하고, 설정 예시는 값이 비어 있는 템플릿으로 제공한다.

## 10. 포트폴리오와 경력서 전략

이 프로젝트에서 전달할 메시지는 다음 한 문장이다.

> 개인 문서 RAG를 구현하는 데 그치지 않고, 실제 사용 질문의 평가 세트로 검색·인용·답변 보류 실패를 측정하고 hybrid retrieval, reranking, 근거 중심 UX로 개선했다.

### 10.1 외부에 보여 줄 증거

이 프로젝트가 경력서 한 줄로 끝나지 않으려면 다음 산출물이 필요하다.

- 누구나 실행할 수 있는 예시 문서·평가 세트·재현 명령
- 기준선과 개선안의 동일 조건 비교 표
- 검색 실패, 잘못된 인용, 불필요한 답변 강행을 각각 설명하는 실패 사례
- 증분 색인·원문 열기·인용 복사까지 보이는 짧은 데모 영상 또는 GIF
- 실제 업무 사용성 측정의 **익명화된 집계 결과**와, 그 결과로 내린 제품 결정

실제 회사 데이터가 없더라도 공개 예시 데이터로 기술 평가를 재현할 수 있어야 한다. 실제 회사 데이터가 있을 때의 사용성 결과는 문서 내용 없이 측정 방법과 집계값만 공개한다.

### 10.2 경력서 문장 템플릿

아래 대괄호는 실제 측정 뒤에만 채운다. 측정하지 않은 숫자나 효과는 쓰지 않는다.

> **Personal Work Document RAG** — 개인 업무 문서 탐색 도구를 설계·구현. 50개 라벨링 질의 기반 평가에서 vector-only 대비 hybrid retrieval + reranking의 Recall@5를 **[A]% → [B]%**로 개선하고, 인용 정확도·답변 보류율을 측정했다. 실제 업무 과제 **[N]개**에서 원문 확인 완료 시간을 **[X]분 → [Y]분**으로 비교 검증했으며, 모든 답변에 추적 가능한 파일·페이지 인용을 제공했다.

수치가 아직 없을 때는 다음처럼 사실만 쓴다.

> **Personal Work Document RAG** — PDF·DOCX·Markdown 대상 hybrid retrieval, 근거 인용, 답변 보류를 갖춘 로컬 문서 탐색 도구를 구현하고, 50개 라벨링 질의로 검색 및 인용 품질을 평가했다.

### 10.3 면접에서 설명할 순서

면접에서는 예쁜 데모보다 다음을 우선해 설명한다.

1. 어떤 개인 지식 탐색 문제를 해결했는가
2. 초기 기준선이 어디에서 실패했는가
3. 그 실패를 어떻게 데이터와 지표로 측정했는가
4. 어떤 설계 변경을 했고, 품질·비용·지연시간은 어떻게 달라졌는가
5. 실제 개인 업무 흐름에서 시간이 줄었는지 어떻게 검증했는가
6. 어떤 실패가 여전히 남아 있고 다음에 무엇을 검증할 것인가

## 11. 최종 완료 체크리스트

- [ ] 예시 데이터만으로 색인, 검색, 질문 응답, 평가를 재현할 수 있다.
- [ ] 검색 결과와 답변의 모든 인용이 원문 위치로 연결된다.
- [ ] vector-only 기준선과 최소 두 개선안을 같은 질문 세트로 비교했다.
- [ ] 최소 10개 실제 업무 과제에서 기존 방식과 도구 방식의 원문 확인 시간을 기록했다.
- [ ] 문서 부재·문서 충돌·인용 오류의 실패 사례를 공개 가능한 형태로 분석했다.
- [ ] 실제 업무 문서, 경로, 비밀값, 식별 가능한 발췌가 저장소·스크린샷·데모에 포함되지 않았다.
- [ ] README와 경력서 문장이 측정한 결과만 주장한다.
