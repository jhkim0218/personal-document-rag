# 개인 업무 문서 RAG — 개선점과 확장 로드맵

작성일: 2026-09-05

## 1. 목적과 현재 판단

이 프로젝트의 목표는 두 가지다.

1. 회사 PC에서 개인 업무 문서를 다시 찾고 원문을 확인하는 시간을 줄인다.
2. 문제 정의, 구현, 실패 분석, 개선 효과를 설명할 수 있는 경력 프로젝트로 발전시킨다.

현재 구현은 로컬 문서 RAG의 초기 프로토타입이다. 네 가지 문서 형식의 파싱, 증분 색인, 검색, 답변, API 연동과 테스트가 있지만, 업무용 완성도와 평가의 신뢰성을 보강해야 한다. 기능 개수를 늘리기 전에 데이터 일관성, 원문 확인, 답변 품질, 평가의 정확성을 우선한다.

이 문서는 현재 소스를 읽고 작성한 개선 계획이며 기능 수정 결과가 아니다. 과거의 테스트 통과나 완료 게이트만으로 설계서 전체의 완료를 증명할 수 없다. 이번 작성에서는 실제 API 호출, 브라우저 조작, 회사 문서 사용성 실험을 재실행하지 않았다.

## 2. 현재 구현과 근거

| 영역 | 현재 상태 | 확인한 코드 |
| --- | --- | --- |
| 문서 입력 | 실행 시 한 개 `--data` 폴더 지정, 하위 폴더 재귀 탐색 | `app.py:main`, `rag/index.py:index_directory` |
| 파싱 | PDF 페이지, DOCX 문단, Markdown 헤딩, TXT 본문 | `rag/documents.py` |
| 색인 | SQLite 저장, 내용 해시와 embedding 모드 비교, 삭제 문서 제거 | `rag/index.py` |
| 검색 | hash/API 벡터, 직접 구현한 BM25, RRF 결합 | `rag/text.py`, `rag/embeddings.py`, `rag/index.py:search` |
| 재정렬 | 단어 겹침과 정확 구문에 가산점 부여 | `rag/index.py:search` |
| 답변 | API 생성 또는 상위 문단 첫 문장 발췌 | `rag/answer.py` |
| 인용 검증 | 번호 존재 및 범위 검사 | `rag/answer.py:validate_citations` |
| 충돌 표시 | 숫자 질문에서 여러 문서의 서로 다른 수치를 발견하면 경고 | `rag/answer.py:detect_possible_conflict` |
| 웹 UI | 색인 버튼, 검색/질문, 결과 카드, 복사 버튼, 파일 링크 | `app.py:PAGE` |
| 평가 | 예시 문서 20개, 질의 50개, 세 검색 설정 비교 | `data/`, `scripts/run_eval.py`, `results/evaluation.json` |

hash embedding은 학습된 의미 임베딩이 아니고, 현재 reranker는 학습 모델이 아닌 규칙 기반 재정렬이다. 두 가지를 포트폴리오에서 정확히 구분해야 한다.

## 3. P0 — 실사용 전에 해결할 문제

### P0-1. 검색 대상 폴더와 인덱스 범위를 일치시키기

- **현재:** 색인 갱신은 `source_root`를 기준으로 하지만 검색은 `SELECT * FROM chunks`로 DB 전체를 읽는다.
- **문제:** 같은 DB로 A 폴더를 색인한 후 B 폴더로 실행하면 A의 문서가 남아 검색될 수 있다. 현재 `--data` 설명과 검색 결과 범위가 어긋난다.
- **개선:** 활성 소스 범위를 검색·상태·출처 조회에 일관되게 적용한다. 가장 작은 수정은 현재 폴더 기준 필터이며, 이후 다중 폴더를 지원할 때 활성 소스 목록으로 확장한다.
- **완료 기준:** 같은 DB에 A/B를 차례로 색인해도 B 모드에서 A 문서가 검색되지 않는다. UI의 문서 수와 검색 범위가 일치한다.

### P0-2. 색인 실패 시 기존 문서를 보존하기

- **현재:** `_replace_document`는 기존 청크를 삭제한 뒤 새 문서를 등록하고 embedding을 요청한다. 파일별 rollback 경계가 없다.
- **문제:** embedding 요청 실패 등을 파일 실패로 기록하고 마지막에 commit하면, 새 청크가 없는 상태를 저장하거나 기존 검색 내용을 잃을 수 있다.
- **개선:** 파싱·청킹·embedding을 먼저 성공시킨 후 파일 단위 트랜잭션으로 교체한다. 실패한 파일은 기존 버전을 보존하고 갱신 실패 상태를 남긴다.
- **완료 기준:** embedding 실패를 주입해도 이전 청크가 검색되고 실패 목록에 나타난다. 재시도 성공 후에만 새 버전으로 바뀐다.

### P0-3. 동시 요청과 오류 복구 정리

- **현재:** `ThreadingHTTPServer`가 단일 SQLite 연결을 공유하고, `check_same_thread=False` 외에 동시 접근 제어는 없다. 색인 버튼 중복 클릭과 파싱/XML/API 오류의 일관된 처리가 부족하다.
- **개선:** 개인용 규모에 맞게 색인 작업을 한 번에 하나로 제한하고 DB 접근을 직렬화하거나 요청별 연결을 사용한다. 오류를 파일 처리, API 요청, 사용자 입력으로 구분해 명확한 응답을 반환한다.
- **완료 기준:** 중복 색인, 색인 중 검색, 손상 DOCX/PDF, API timeout을 재현했을 때 DB가 일관되게 유지되고 실패 원인이 UI에 표시된다.

### P0-4. 실제로 원문 위치까지 도달하는 UI

- **현재:** 검색 결과에 `file://` 링크는 있지만 페이지·헤딩으로 이동하는 처리가 없다. 질문의 출처에는 원문 링크와 복사 버튼이 없다. 현재 웹 테스트는 HTTP 응답만 검사한다.
- **개선:** 브라우저 내 근거 뷰어를 제공해 인용 문단으로 이동하고, PDF는 페이지 위치를 연결한다. 검색과 질문에 같은 출처 카드를 사용한다. 원본 파일 열기는 지원 환경에서 별도로 검증한다.
- **경계:** 원문 제공 기능은 인덱스에 등록된 문서 ID로만 접근하게 한다. 사용자가 보낸 임의 경로를 그대로 열거나 실행하지 않는다.
- **완료 기준:** 실제 브라우저에서 검색 → 근거 위치 확인 → 인용 복사가 성공한다. 질문에서도 동일하게 동작한다. 파일명을 아는지만 검사하는 HTTP 테스트로 대체하지 않는다.

### P0-5. 평가 이름과 실제 측정 대상을 일치시키기

- **현재 `grounded_answer_rate`:** 인용된 source가 하나라도 있으면 점수를 올린다. 주장이 원문에 의해 지지되는지는 평가하지 않는다.
- **현재 `citation_precision`:** 인용 파일명이 정답 파일명과 일치하는 비율이다. 문장별 인용의 정확성을 뜻하지 않는다.
- **현재 `retrieval_recall_at_5`:** 단일 정답 파일을 상위 5개 청크에서 찾았는지 계산한다. 다중 정답 근거의 recall을 측정하지 않는다.
- **현재 비용:** `0.0`이 고정값인데, 평가용 `RAGIndex`는 환경의 API 키를 읽는다. 키가 있으면 실제 embedding 호출과 비용이 발생할 수 있다.
- **개선:** 기존 지표는 `citation_presence_rate`, `gold_document_citation_rate`, `document_hit_at_5` 등으로 정확히 이름 붙인다. 별도로 주장별 근거 지지 여부, 인용 coverage, 다중 근거 recall을 평가한다. 평가 모드를 명시하고 offline은 네트워크 호출을 차단한다.
- **완료 기준:** 정답 파일에서 엉뚱한 문장을 인용한 답은 의미적 인용 평가에서 실패한다. 거짓 주장에 `[1]`만 붙여도 grounded 점수를 받지 않는다. API 키가 있어도 offline 평가가 외부 호출을 하지 않는다.

## 4. P1 — 매일 쓰기 위한 개선

### P1-1. 폴더 범위를 화면에서 설정하고 확인하기

- **필요성:** 사용자가 문서를 옮기거나 명령줄 옵션을 기억하지 않고 기존 업무 폴더를 그대로 쓸 수 있어야 한다.
- **추가:** 폴더 경로 목록, 포함/제외 하위 폴더, 확장자 필터, 선택 범위 미리보기, 설정 저장.
- **선행 조건:** P0-1의 검색 범위 일관성 해결.
- **완료 기준:** 서로 다른 두 폴더를 추가하고 하나를 비활성화하면 실제 검색 결과와 문서 수가 즉시 해당 범위를 따른다. `.git`, 임시 파일, 앱 캐시 등 제외 범위가 미리보기와 일치한다.

### P1-2. 증분 색인의 체감 속도 개선

- **현재:** `parse_document`를 실행한 뒤 해시를 비교하므로 변경되지 않은 PDF도 다시 파싱한다.
- **개선:** 파일 메타데이터와 해시 확인을 파싱보다 먼저 수행한다. 엄격한 확인 모드에서는 메타데이터가 같아도 해시를 확인한다. 파일 처리 상태와 마지막 성공한 색인 작업 시각을 별도로 저장한다.
- **추가 UX:** 색인 진행률, 처리 중 파일, 실패 목록, 실패 파일 재시도, 명시적 전체 재색인.
- **완료 기준:** 변경 없는 재색인에서 파서와 embedding 호출이 모두 0회다. 한 파일 수정 시 그 파일만 갱신한다. 파싱 방식/청킹 버전 변경 시 필요한 재색인이 수행된다.

### P1-3. 검색 중 불필요한 계산 줄이기

- **현재:** 매 검색마다 전체 벡터 JSON을 읽고 전체 문서 BM25 통계를 다시 계산한다. FTS 테이블은 생성·갱신하지만 검색에 사용하지 않는다. keyword 모드도 query embedding을 요청한다.
- **개선:** keyword 모드의 embedding 호출을 없앤다. BM25 통계와 벡터를 색인 버전별로 재사용한다. FTS는 한국어 검색 품질을 검증해 실제 후보 검색에 사용하거나 중복 저장을 제거한다.
- **확장 조건:** 실제 코퍼스에서 전체 벡터 순회가 병목이면 벡터 인덱스를 도입한다. 문서 수만 보고 별도 DB 서비스를 먼저 추가하지 않는다.
- **완료 기준:** keyword 검색은 API 없이 동작하고, 여러 코퍼스 크기의 p50/p95·메모리·색인 시간을 기록한다. 최적화 후 동일 평가 질의에서 검색 품질이 유지된다.

### P1-4. 한국어와 정확한 식별자 검색 개선

- **현재:** 정규식 토큰 분리와 소수 불용어만 사용한다. 조사 결합 표현이 서로 다르게 처리되고 한 글자 한글은 모두 제거된다. 제목과 경로도 검색 점수에 직접 반영하지 않는다.
- **개선:** 먼저 한국어 표현 변형, 띄어쓰기, 한영 용어, 에러 코드, 파일명 질의로 실패 세트를 만든다. 제목/경로 검색을 추가하고, 필요하면 형태소 처리 또는 문자 n-gram을 비교한다.
- **완료 기준:** `배포 일정`과 `배포일정은`, 약어와 정식 명칭, 정확한 파일명·코드 질의에서 개선을 측정한다. 일반 QA 개선 때문에 정확 코드 검색이 나빠지지 않도록 별도 보고한다.

### P1-5. 답변 가능성 판단과 인용 개선

- **현재:** 단어 하나라도 겹치면 답변 후보로 인정한다. 의미 검색으로 좋은 문서를 찾아도 단어가 겹치지 않으면 버릴 수 있다. API에는 청크 앞 500자만 전달하며 fallback은 첫 문장만 고른다.
- **개선:** 질문을 해결할 근거인지 판단하고, 답을 지지하는 문장을 선택한다. 화면 발췌 길이와 모델 컨텍스트 길이를 분리한다. 부분 답변과 불충분한 항목을 구분한다.
- **인용:** 문장 또는 주장 단위로 출처를 연결한다. 인용 번호 존재 검사와 내용 검증을 별도 단계로 유지한다.
- **완료 기준:** 관련 문서지만 요청한 사실은 없는 질문에서 답변을 보류한다. 청크 뒤쪽에 있는 정답도 인용한다. 인용을 누르면 실제 지지 문장이 보인다.

### P1-6. 문서 충돌과 최신성 처리

- **현재:** 같은 주제의 같은 속성인지 확인하지 않고 서로 다른 숫자가 있으면 충돌로 본다. 서로 다른 정상 지표도 경고할 수 있다.
- **개선:** 프로젝트/대상, 속성, 값, 적용 시점을 함께 비교한다. 파일 수정 시각과 문서의 결정 날짜를 구분한다. 명확한 근거가 없으면 최신 결정을 단정하지 않는다.
- **완료 기준:** 서로 다른 두 성능 지표는 충돌로 분류하지 않고, 같은 배포 일정의 상충 기록은 양쪽 인용으로 보여준다. 명시적인 변경 기록이 있으면 변경 전후를 설명한다.

### P1-7. 실행·대기·오류 상태를 명확하게

- **추가:** 실행 후 브라우저 자동 열기 옵션, 현재 폴더와 검색 모드 표시, Enter 검색, 처리 중 상태, 중복 클릭 방지, 복사 성공/실패 알림.
- **모드:** API 키 유무에 따른 외부 전송을 사용자가 명확히 알 수 있도록 검색/생성 모드를 표시한다. 완전 로컬 모드는 키가 있어도 명시적으로 선택할 수 있게 한다.
- **API 안정성:** 청크 묶음 크기 제한, 일시 오류 재시도, 요청 시간 제한, usage 기록을 추가한다. 모델 변경 후에는 불일치 벡터로 검색하지 않고 재색인을 안내한다.
- **완료 기준:** 요청 실패 후 화면이 무기한 대기하지 않고 재시도 경로를 제공한다. offline 모드의 네트워크 미사용을 테스트한다.

## 5. 평가와 경력서 증거 보강

### 5.1 현재 숫자의 해석

아래 값은 저장된 `results/evaluation.json`을 확인한 것이며 이번 문서 작성 시 새로 측정한 값은 아니다.

| 설정 | 상위 5개에서 정답 파일 발견 | 정답 파일 인용 비율* | 답변 불가 질의 보류 |
| --- | ---: | ---: | ---: |
| vector-only | 72.3% | 53.2% | 3/3 |
| BM25 | 95.7% | 76.5% | 3/3 |
| hybrid + 규칙 재정렬 | 95.7% | 70.6% | 3/3 |

*현재 코드의 파일명 일치 기준이다. 의미적 인용 정확도로 해석하면 안 된다.

이 결과만으로 hybrid가 BM25보다 우수하다고 말할 수 없다. 보류 100%도 비현실적 고유명사 등을 포함한 3문항에서 나온 결과다. 실무의 어려운 미답변 질문을 잘 처리한다는 증거가 되지 않는다. 저장된 보고서에는 embedding 모델·코퍼스 hash 등 실행 조건도 충분하지 않아 재현 메타데이터를 보강해야 한다.

### 5.2 평가 데이터 개선

- 자체 RAG 설명 문서 위주 코퍼스를 회의록, 결정 변경 기록, 운영 매뉴얼, 기술 스펙 등의 가상 업무 자료로 확장한다.
- 단일 파일 정답뿐 아니라 여러 문서가 필요한 비교, 같은 키워드가 있지만 답은 없는 경우, 버전 충돌, 긴 표, 동의어 질의를 포함한다.
- 정답 라벨에 문서 ID, 근거 문장/위치, 필수 사실, 답변 가능 여부, 질문 유형을 기록한다. 평가용 정답 근거는 생성된 chunk ID에만 의존하지 않는다.
- 개발용 질의와 최종 확인용 질의를 분리한다. 시스템을 고치기 위해 확인한 질의를 독립적인 최종 성능 증거로 재사용하지 않는다.
- 문서와 라벨은 AI 초안인지 사람이 검수했는지 구분해 기록한다. 현재 50문항도 실제 사람이 전부 검수했다는 근거 없이 ‘사람이 라벨링했다’고 주장하지 않는다.

### 5.3 설계서에 있지만 아직 충분히 실행되지 않은 실험

| 실험 | 남은 일 | 남길 증거 |
| --- | --- | --- |
| E1 검색 방식 비교 | 같은 embedding 모드·청킹·재정렬 조건에서 검색 방식만 변경 | 질의별 결과, 설정, 데이터 hash |
| E2 청킹 비교 | 고정/구조 기반 전략을 실제로 선택 가능하게 하고 비교 | 동일 원문 근거 기준 recall, 긴 문서 실패 사례 |
| E3 reranker 비교 | hybrid의 재정렬 on/off를 독립적으로 비교 | 정확도·지연시간·비용 변화 |
| E4 인용·보류 비교 | 답변 생성 정책별 비교, 의미적 인용/거짓 답변 평가 | 검수표, 실패 사례, 정책 선택 이유 |

실험 결과에는 모델 ID, 코드 버전, 코퍼스/질의 hash, 청킹 설정, top-k, 프롬프트 버전, 실행 시점, API usage를 포함한다. 비용 계산 단가와 기준일도 보존한다. 계측할 수 없는 비용은 `null`/미측정으로 표시한다. 전체 답변 시간은 검색을 포함한 사용자 대기 시간으로도 측정한다.

### 5.4 실제 사용 효과 기록

추천 실사용 시나리오는 ‘회의록·스펙에서 과거 결정의 근거 찾기’다. 반복되는 업무 하나에서 시작하면 효과와 실패를 구체적으로 설명할 수 있다.

- 2주 동안 실제 사용 질문과 실패 유형을 로컬에 기록한다.
- 대표 과제 10~15개에서 기존 탐색과 도구 사용의 원문 확인 완료 시간, 성공률, 답변 수정 필요 여부를 비교한다.
- 같은 과제를 반복해서 생기는 학습 효과를 줄이도록 순서를 섞거나 난도가 비슷한 과제 쌍을 사용한다.
- ‘빨라졌지만 틀린 답’은 성공으로 처리하지 않는다. 단일 사용자 사례라는 한계를 보고한다.
- 공개 자료에는 허용된 집계값과 가상 데이터로 재현한 실패 사례만 넣는다.

경력서에는 ‘개인 업무 도구 구현’, ‘실제 업무에서 개인 사용’, ‘팀/고객 도입’을 구분한다. 개인 사용만 검증했으면 조직에 배포했다고 쓰지 않는다. 문서 검색부터 근거 검증까지의 구체적인 개선과 선택 이유가 핵심이다.

## 6. P2 — 기반이 안정된 뒤 확장할 요소

| 확장 | 사용자에게 주는 가치 | 시작 조건 | 완료 기준 |
| --- | --- | --- | --- |
| 결정 이력 비교 | ‘언제 무엇이 왜 바뀌었나’를 두 문서에서 확인 | 같은 프로젝트의 결정 문서를 자주 찾음 | 날짜·변경 전후·근거를 묶고 추측은 표시 |
| 관련 문서 묶음 | 한 질문에 필요한 스펙·회의록·런북을 함께 찾음 | 다중 문서 질문에서 누락이 반복됨 | 필요한 근거 문서의 coverage 개선 |
| 로컬 학습 임베딩 | 외부 API 없이 의미 검색 품질 개선 | 외부 전송 없는 사용이 실제 요구됨 | 설치 크기·메모리·품질·속도 비교 및 네트워크 미사용 확인 |
| 학습된 reranker | 비슷한 후보 사이의 관련성 판단 개선 | 규칙 재정렬의 실패가 충분히 수집됨 | 품질 향상이 추가 대기 시간/비용을 정당화 |
| PPTX/XLSX/HWP 등 | 실제 업무 문서의 누락 감소 | 자주 찾는 자료가 미지원 형식임 | 슬라이드/시트/셀/절 위치를 보존한 인용 |
| OCR | 스캔 문서를 검색 가능하게 | 이미지 PDF 때문에 실패가 누적됨 | 페이지 위치·추출 오류를 표시하고 OCR 품질 검수 |
| 선택 폴더 자동 갱신 | 갱신 버튼을 누르는 수고 감소 | 수동 갱신 누락이 실제 불편으로 확인됨 | 저장 중 파일 보호, 이벤트 합치기, 재시도 및 중지 |
| 개인 평가 화면 | 잘못된 답을 개선 데이터로 축적 | CLI 평가만으로 실패 검토가 번거로움 | 근거·답변·판정·실험 차이를 한 화면에서 검토 |
| 경량 개념 관계 | 프로젝트·결정·문서 연결 탐색 | 일반 검색으로 풀리지 않는 관계 질의가 반복됨 | 관계 추가 전후 multi-document recall 비교 |

온톨로지는 `프로젝트 → 결정 → 근거 문서`처럼 업무에 필요한 관계부터 시작한다. 초기에 그래프 DB나 범용 온톨로지 체계를 도입할 필요는 없다. 여러 기능 중 선택한다면 P0/P1 이후 **결정 이력 비교**를 우선 권장한다. 사용자의 반복 업무와 연결하기 쉽고, 여러 출처와 최신성을 다루는 문제 해결 과정을 보여줄 수 있기 때문이다.

## 7. 구현 순서와 단계별 종료 조건

| 단계 | 범위 | 종료 조건 |
| --- | --- | --- |
| A. 신뢰 가능한 기반 | P0-1~5 | 폴더 혼합·색인 실패·동시 요청 회귀 테스트, 실제 인용 UX, 지표 정의 수정 |
| B. 일상 사용 | P1-1~7 | 실제 폴더 설정, 빠른 재색인, 한국어 실패 개선, 모드·오류 표시, 네트워크 제어 |
| C. 성능 입증 | E1~E4 및 실제 사용 기록 | 분리된 평가 질의, 검수 결과, 5개 이상 실패 분석, 업무 시간 비교 |
| D. 차별화 | P2에서 한 가지 선택 | 사용성 또는 품질 개선을 확인한 기능만 채택 |

각 단계에서 구현 기록에 ‘무엇을 / 왜 / 어떻게 / 검증 결과 / 남은 한계’를 추가한다. 완료 체크는 테스트 개수가 아니라 해당 동작을 직접 확인하는 증거를 기준으로 한다. 테스트 일부 통과와 파일 존재 검사는 설계 전체 완료와 구분한다.

## 8. 다음 작업으로 권장하는 묶음

가장 먼저 **검색 폴더 범위 일치 + 실패에 안전한 증분 색인 + 원문 근거 뷰어 + 평가 지표 정정**을 함께 개선한다. 업무에서 믿고 쓰는 기반과 경력서에 쓸 수 있는 검증 자료를 동시에 만들 수 있다.

조직 권한, 멀티테넌시, 사내 커넥터, 자동 문서 수정은 기존 개인용 범위 밖으로 유지한다. 새 기능은 실제 사용에서 수집한 실패나 시간 낭비를 해결할 때 추가한다.
