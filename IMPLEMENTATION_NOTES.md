# 구현 기록 — Personal Work Document RAG

> 이 문서는 초기 구현의 기록입니다. 이후 변경된 검색 범위·실패 복구·근거 UI·평가 정의는 [개선 결정 기록](IMPROVEMENT_DECISIONS.md)과 README를 우선합니다. 특히 아래의 초기 인용 정확도·grounded 평가 표현은 의미적 품질 검증으로 해석하면 안 됩니다. 최신 평가는 해당 의미 지표를 미측정(null)으로 표시합니다.

이 문서는 구현한 기능, 해당 기능이 필요한 이유, 구현 방식, 검증 방법을 분리해 기록한다. 실제 업무 문서나 측정되지 않은 성과 수치는 포함하지 않는다.

## 1. 로컬 문서 수집과 위치 보존

### 무엇을 구현했는가

PDF, DOCX, Markdown, TXT를 읽어 검색 가능한 청크로 만들었다. 각 청크는 문서 경로, 제목, 페이지·헤딩·문단 위치, 본문, content hash를 가진다.

### 왜 구현했는가

파일명 검색만으로는 “그 내용을 어디서 봤지?”라는 업무 질문을 해결하지 못한다. 특히 RAG 답변은 원문을 다시 확인할 수 있어야 하므로 단순 텍스트만 저장하면 부족하다.

### 어떻게 구현했는가

- `rag/documents.py`가 형식별 파서를 제공한다.
- PDF는 `pypdf`로 페이지별 텍스트를 추출한다.
- DOCX는 외부 문서 편집 라이브러리 없이 ZIP 내부의 WordprocessingML 문단을 읽는다.
- Markdown은 헤딩을 위치로 보존하고, TXT는 단일 text 위치로 처리한다.
- `chunk_text`는 기본 900자·160자 중첩을 쓰되 문단 경계를 먼저 보존한다.

### 어떻게 검증했는가

`tests/test_documents_and_index.py`가 네 형식의 임시 파일을 만들고, 각 파일이 비어 있지 않은 청크와 올바른 위치 접두어를 만드는지 확인한다.

## 2. 증분 색인과 로컬 저장

### 무엇을 구현했는가

변경 파일만 다시 색인하고, 선택한 폴더에서 사라진 파일은 인덱스에서 제거하는 SQLite 기반 인덱스를 구현했다. 원본 파일은 읽기만 한다.

### 왜 구현했는가

실제 업무 폴더는 문서가 많고 자주 바뀐다. 매번 전부 임베딩하면 느리고 비용이 들며, 원본을 변경하면 개인 도구의 신뢰를 해친다.

### 어떻게 구현했는가

- `rag/index.py`는 SHA-256 content hash로 이전 색인 상태와 비교한다.
- `documents`와 `chunks` 테이블을 분리하고, 청크에는 위치와 embedding JSON을 저장한다.
- 같은 내용이 다른 경로에 있는 경우도 별도 문서로 유지하도록 경로와 hash를 함께 document id에 반영했다.
- 색인 요청은 `indexed`, `skipped`, `removed`, `failed`를 반환한다.

### 어떻게 검증했는가

자동 테스트가 첫 색인, 변경 없는 재실행, 파일 수정, 파일 삭제를 차례대로 수행해 색인·건너뜀·제거 수와 최종 문서 수를 확인한다.

## 3. 검색: 기준선과 개선안

### 무엇을 구현했는가

세 검색 설정을 만들었다: vector-only, BM25 keyword-only, hybrid retrieval + reranking.

### 왜 구현했는가

RAG 품질은 “벡터 검색을 넣었다”로 증명되지 않는다. 제품명·약어·에러 코드는 키워드 검색이 강하고, 표현이 다른 개념 질의는 벡터 검색이 도움이 될 수 있다. 같은 질의 세트에서 비교해야 선택에 근거가 생긴다.

### 어떻게 구현했는가

- `OPENAI_API_KEY`가 있으면 색인 시 Embeddings API로 문서 청크와 질의의 vector를 생성한다. embedding 모델 또는 모드가 바뀌면 같은 파일도 다시 색인해 서로 다른 vector 공간이 섞이지 않게 한다.
- 키가 없으면 안정적인 해시 기반 token/character n-gram vector와 cosine similarity를 사용한다. API 키가 없어도 재현 가능한 기준선이 필요해서다.
- keyword 검색은 `rag/text.py`의 in-process BM25로 계산한다.
- hybrid는 양쪽의 상위 후보를 reciprocal rank fusion으로 합친다.
- reranker는 질문 token coverage와 정확 문구 포함 여부로 상위 후보를 가볍게 재정렬한다. 이 단계는 의도적으로 단순하며, 외부 cross-encoder가 필요한 규모가 되기 전까지는 서비스 의존성을 늘리지 않는다.

### 어떻게 검증했는가

`data/sample/`의 직접 작성한 문서 20개와 `data/eval/questions.jsonl`의 50개 라벨링 질의를 `scripts/run_eval.py`로 실행한다. 스크립트는 세 설정의 Recall@5, 인용 정확도, grounded answer rate, appropriate abstention, 검색/답변 p50·p95 지연시간과 질의별 상위 결과를 JSON으로 남긴다. 공개 평가의 비용은 API를 호출하지 않아 `0.0`이며, 실제 API 비용을 임의로 추정하지 않는다.

## 4. 근거 기반 답변과 답변 보류

### 무엇을 구현했는가

검색 결과의 근거 문단만 사용해 답변하고, 근거가 없으면 보류한다. 모든 답변은 파일·위치·발췌를 가진 source 목록을 함께 반환한다.

### 왜 구현했는가

개인 업무 도구의 중요한 실패는 틀린 답변보다 틀렸는데 맞는 것처럼 보이는 답변이다. 사용자가 원문으로 이동해 빠르게 검증할 수 있어야 한다.

### 어떻게 구현했는가

- `rag/answer.py`는 검색 결과에 질문과 겹치는 근거가 없으면 `abstained` 상태를 반환한다.
- API 키가 없을 때는 가장 강한 근거 한 개(충돌 후보면 두 개)의 문장을 `[1]` 형태로 인용하는 추출형 답변을 만든다. 관련성이 낮은 상위 후보를 무조건 함께 인용하지 않기 위해서다.
- `OPENAI_API_KEY`가 설정되면 Responses API에 질문과 **상위 근거 발췌만** 전달한다. 프롬프트는 근거 밖 사실 금지, 문장별 인용, 근거 부족 시 보류를 요구하고 `store: false`를 설정한다.
- 모델 출력의 인용 번호가 없거나 범위를 벗어나면 안전한 추출형 답변으로 fallback한다.
- 날짜·수치를 묻는 질문에서 서로 다른 문서가 서로 다른 날짜·수치를 제시하면 `conflict` 상태로 표시해 원문 검토를 요구한다.

### 어떻게 검증했는가

`tests/test_answer.py`가 fallback 인용, 근거 없는 질문의 보류, 잘못된 인용 번호의 거부, 경쟁하는 날짜 주장 표시를 검증한다. 실제 키 없이 OpenAI Embeddings/Responses 요청의 URL·payload·`store: false`·출력 순서를 mock HTTP 응답으로 검증한다. 실제 API 호출의 품질·비용은 사용자 키와 허용된 문서가 있을 때 별도로 측정한다.

## 5. 실제로 쓰는 UI와 HTTP 인터페이스

### 무엇을 구현했는가

로컬 전용 웹 UI와 JSON HTTP API를 구현했다. 상태 확인, 변경 파일 색인, 검색, 질문, 출처 메타데이터 조회가 가능하다.

### 왜 구현했는가

업무 효율은 검색 결과를 보고 원문을 여는 왕복을 줄일 때 생긴다. CLI 결과만으로는 원문 열기와 인용 복사 흐름을 검증하기 어렵다.

### 어떻게 구현했는가

- `app.py`는 Python 표준 라이브러리 `ThreadingHTTPServer`만 사용해 `127.0.0.1`에만 바인딩한다.
- 검색 카드에는 제목, 위치, 발췌, 원문 `file://` 링크, 인용 복사 버튼이 있다.
- `/api/index`, `/api/status`, `/api/search`, `/api/ask`, `/api/source`가 UI와 외부 확인에 필요한 최소 API다.

### 어떻게 검증했는가

`tests/test_web.py`와 `scripts/smoke_web.py`가 임시 로컬 서버를 열고 색인→상태→검색→질문→출처 조회를 순서대로 요청한다.

## 6. 실제 업무 효율과 경력서 증거

### 무엇을 구현했는가

실제 업무 데이터는 저장소에서 분리하고, 사용자가 회사 PC에서 수행할 시간 측정 절차를 설계서와 README에 명시했다.

### 왜 구현했는가

기술 평가 점수만으로는 개인 업무에 유용한지 알 수 없고, 공개 프로젝트에 업무 데이터를 넣는 것은 허용되지 않는다. 경력서에는 관찰하지 않은 생산성 수치를 쓸 수 없다.

### 어떻게 구현했는가

- 기존 파일 탐색과 도구 사용을 대표 과제 10~15개에서 비교한다.
- 시작부터 원문을 열어 확인할 때까지의 시간, 성공 여부, 수정 필요 여부를 기록한다.
- 공개물에는 데이터 내용 대신 익명화된 집계 지표와 일반화한 실패 유형만 넣는다.

### 어떻게 검증하는가

이는 실제 회사 PC와 허용된 문서가 있어야 하는 사용성 검증이다. 코드 테스트로 대체하지 않으며, 수행 전에는 성과를 주장하지 않는다. 측정 후 `docs/case-study.md`에 방법·집계값·한계를 기록한다.

## 7. 의도적으로 구현하지 않은 것

- 조직 권한, 사내 커넥터, 멀티테넌시: 개인 단일 사용자 범위 밖이다.
- 자동 스케줄러와 에이전트형 문서 수정: 증분 수동 색인만으로 현재 문제를 해결한다.
- 무거운 온톨로지/지식 그래프: 검색 실패를 실제로 줄인다는 평가 증거가 생길 때만 추가한다.

이 제외 항목은 누락이 아니라, 개인용 MVP가 실제로 쓰기 쉽고 포트폴리오에서 실험의 인과관계를 설명하기 쉽게 유지하기 위한 범위 결정이다.
