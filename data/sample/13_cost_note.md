# 비용 관찰 노트

비용은 임베딩, reranking, 답변 생성으로 나누어 질의당 평균 비용을 기록한다. API 키가 없을 때는 로컬 hash embedding과 추출형 답변으로 동작하지만, 의미 검색과 자연스러운 종합 품질은 낮아질 수 있다.

OpenAI API 키를 설정하면 Responses API로 근거 문단만 전달해 짧은 답을 생성한다. 원본 전체 문서를 매 질의마다 전송하지 않는다.
