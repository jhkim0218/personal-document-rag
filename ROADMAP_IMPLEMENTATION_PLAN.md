# 전체 로드맵 구현 추적

## 진행 기록 — 명시적 프로젝트·결정·문서 관계

- 무엇: `.relations.json`에 사람이 입력한 `project → decision → indexed document paths`를 보관하고, 프로젝트/결정 이름으로 탐색해 기존 source 링크를 반환하는 API와 UI를 만들었다.
- 왜: 회의록·스펙·런북의 관계를 모델이 추측하게 하면 잘못된 연결이 이력/답변에 섞일 수 있다. 반복되는 업무 관계부터 명시적으로 기록하면 가벼운 온톨로지의 검토 가능한 시작점이 된다.
- 범위/검증: 설정 저장 시 문서가 현재 활성 source에 있고 색인됐는지 검사한다. 같은 프로젝트/결정 이름 중복과 미색인·범위 밖 경로를 거절하고, 폴더 비활성화 뒤에도 관계 설정은 남지만 문서 링크가 숨겨지는 것을 테스트했다. HTTP 설정·검색도 검증했다.
- 한계: relation은 수동 메타데이터이며 자동 entity resolution/관계 추출/시간 적용성 판단이 아니다. 후보 문서가 질문의 모든 근거를 덮는지나 관계 전후 multi-document recall 개선을 측정하지 않았으므로 P2-RELATIONS 전체 완료가 아니다.

## 진행 기록 — 영속 API 사용량 저널

- 무엇: 기존 API attempt의 content-free 필드(시각·모델·엔드포인트·시도·상태·지연·응답 토큰 수·미측정 비용)를 `*.usage.sqlite3`에 기록하고 `/api/status`의 `api_usage`와 화면 상태에 노출했다. 프로세스 메모리 최근 100회 로그는 그대로 유지한다.
- 왜: 재시작하면 호출 이력을 잃으면 모델 설정·오류·토큰 사용량을 재현 보고서나 비용 점검에 연결하기 어렵다. 반대로 원문·질문·응답 본문·키를 사용량 로그에 넣으면 개인 문서 도구의 개인정보 범위를 불필요하게 넓힌다.
- 검증: mock API 응답의 모델/토큰 usage가 재시작 뒤에도 남고 request payload의 `secret` 값은 SQLite에 없는지 검사했다. offline 상태의 persistent usage 활성, 기존 재시도 테스트도 실행했다.
- 한계: 최근 100건만 화면에 표시하며 보존 기간/용량 상한·가격표 연결·실제 비용 추정·전체 스트리밍 응답 바이트/벽시계 제한은 아직 없다. usage가 제공되지 않으면 null이며 사용량 기록은 응답 품질을 뜻하지 않는다.

## 진행 기록 — 개인 답변·근거 검수 UI

- 무엇: 질문으로 만든 답변을 별도 로컬 SQLite에 저장하고 원문 확인 후 supported/partial/unsupported으로 한 번 판정하는 화면과 `/api/reviews` 경로를 구현했다. 각 항목은 질문, 답변, 표시된 source ID·경로·위치·발췌, 메모와 판정 시각을 가진다. 번호 인용이 있는 문장은 별도 claim 단위로 같은 세 판정을 한 번 저장할 수 있다.
- 왜: AI 작성 개발 fixture의 exact-span 점수만으로는 실제 답변이 충분한지 판정할 수 없다. 사용자가 틀린 답과 부분 답을 근거와 함께 남겨 이후 평가·실패 분석으로 삼을 수 있어야 한다.
- 개인정보: 모델 context 전체는 저장하지 않고 표시 source의 최소 필드만 저장한다. 그래도 질문·답변·경로·발췌·메모 자체가 민감할 수 있으므로 `.reviews.sqlite3`은 Git 제외이며 공개용 데이터가 아니다.
- 검증: source context 제거 저장, answer/claim의 pending→supported 판정, 중복 판정 거절, 재시작 보존을 단위 테스트했다. HTTP에서 질문→저장→claim 판정도 확인했다. 익명 집계 export가 질문·답변·경로·발췌·메모를 제외하는 단위/HTTP 테스트도 추가했다. 실제 사람이 검토한 결과는 넣지 않았다.
- 한계: 사용자가 같은 답변을 반복 저장할 수 있고, 실제 판정자·독립 이중 검수 기록은 아직 없다. `docs/HUMAN_REVIEW_PROTOCOL.md`에 라벨 가이드와 절차를 추가했지만, 익명 export는 count만 내보내므로 claim 단위 evidence 자체를 공개 검증할 수 없다. 따라서 claim 단위 입력 기능은 semantic evaluation/DATA/E4 또는 실제 사용 품질 개선의 증거가 아니다.

## 진행 기록 — 원문 근거·인용 claim 평가

- 무엇: `scripts/evaluate_grounding.py`는 기존 가상 업무 fixture의 문서·헤딩·문장 gold 라벨로 top-5 literal evidence recall, gold 문장을 실제 포함한 표시 인용 coverage, 인용된 답변 문장이 해당 source context에 그대로 있는지 계산한다. 결과에는 semantic metrics를 null로 명시한다.
- 왜: 정답 파일을 찾았거나 `[1]`이 하나 있다는 사실만으로 해당 문장/주장이 근거에 의해 지지된다고 할 수 없다. 같은 파일의 다른 문단과 근거 없는 주장에 유효 번호를 붙인 경우를 별도 실패로 확인해야 한다.
- 검증: 6개 가상 업무 질의/8개 gold 근거의 현재 offline 실행은 literal recall@5 1.0, 표시 인용 coverage 0.875, literal cited-claim support 1.0을 기록했다. 같은 파일의 다른 발췌는 gold 인용이 아니며, `Launch is Friday [1]`에 `Launch is Monday` context를 붙인 대조군은 support 0/1임을 테스트했다. 소켓 연결을 막아 API 요청 0회를 검증했다.
- 한계: label은 AI 작성 개발 fixture이고, exact containment는 패러프레이즈·부정·조건·다문장 추론을 판정하지 못한다. 이 수치를 semantic grounding·사람 검수·실제 업무 품질로 쓰지 않는다. 사람 검토 claim/evidence 라벨과 분리 holdout이 있어야 P0-5/DATA/E4 전체를 완료할 수 있다.

## 진행 기록 — 관련 문서 묶음

- 무엇: `/api/bundle`과 화면 **관련 문서 묶음**은 기존 hybrid 검색 상위 20개에서 각 파일의 최고 순위 청크만 골라 최대 5개의 서로 다른 문서를 표시한다. 원래 검색 순위, 후보 청크 수, 후보/선택 문서 수를 함께 보여 준다.
- 왜: 하나의 긴 회의록이 여러 청크로 상위를 차지하면, 같은 질문에 필요한 스펙·운영 절차 문서를 함께 확인하기 어렵다. 새 관계 모델이나 재정렬 모델을 만들기 전에 가장 작은 문서 다양화로 다중 출처 확인을 지원한다.
- 검증: 중복 청크가 있는 순위에서 각 문서의 첫 청크만 남고 retrieval rank가 보존되는 단위 테스트, 실제 HTTP 색인→bundle 경로에서 중복 path가 없는지 확인하는 테스트를 추가했다.
- 한계: 후보 20개 밖의 문서는 복원하지 않고, 선택된 문서가 질문의 다른 필수 사실을 실제로 덮는지 판정하지 않는다. multi-document coverage 전후 평가와 프로젝트·결정 관계 라벨은 아직 없으므로 P2-BUNDLE/P2-RELATIONS 전체 완료가 아니다.

## 진행 기록 — 결정 이력 비교

- 무엇: 검색 결과의 원문 문장에서 명시적 변경 표현만 추출해 날짜순으로 표시하는 `/api/history`와 화면 **결정 이력 비교**를 추가했다. 각 이벤트에는 원문 문장과 chunk ID·파일·위치를 남긴다.
- 왜: 회의록과 스펙의 변경을 단순 최신 파일명이나 파일 수정시각으로 결정하면 잘못된 결론을 낼 수 있다. 날짜·이전 값·새 값·이유가 직접 쓰인 경우만 한 화면에서 비교하되, 누락된 정보를 자동으로 채우지 않아야 한다.
- 검증: 영어/한국어 날짜와 화살표 변경문을 시간순으로 정렬하고, 원문 링크·변경 전후·이유를 보존하는 테스트를 추가했다. 날짜/값/이유가 없는 변경문은 모두 null로 남으며, 날짜만 있는 비변경 문장은 이력에 포함하지 않는 대조군을 실행했다. HTTP 색인→이력 조회도 검증했다.
- 한계: 표면적인 키워드/정규식 기준이라 같은 프로젝트/결정의 entity resolution, 적용일과 작성일의 구분, 자연어 변경 문장의 일반 해석은 하지 않는다. 여러 문서를 하나의 결정으로 묶는 coverage 측정도 아직 없다. 따라서 P1-6과 P2-HISTORY 전체 완료를 주장하지 않는다.

## 진행 기록 — 업무 문서 형식 확장

- 무엇: 표준 라이브러리 ZIP/XML 파서로 PPTX의 슬라이드 텍스트, XLSX의 시트명·셀 좌표·shared/inline 문자열·저장된 수식 결과, HWPX의 절 텍스트를 추출하고 기존 청킹·색인·출처 위치에 연결했다.
- 왜: 회의 자료와 결정 표가 PDF/DOCX 밖에 있으면 검색 범위가 넓어도 실제 업무 자료가 누락된다. 출처는 파일명만이 아니라 슬라이드/셀/절까지 보여야 원문을 재확인할 수 있다.
- 검증: ZIP fixture로 슬라이드 정렬, 시트 `Decisions`의 A1/B1/C1 위치와 값/수식, HWPX section 1 위치, 세 형식까지 포함한 7개 문서 색인을 검증했다. 기존 소스 설정 테스트도 통과했다.
- 한계: 구형 바이너리 HWP, 암호화 Office 파일, 차트/이미지/OCR, 수식 재계산은 처리하지 않는다. HWPX는 새 XML 기반 형식이며 HWP 자체 지원을 주장하지 않는다. 실제 대형 업무 통합문서의 표 의미/병합 셀 품질은 아직 검수하지 않았다.

## 진행 기록 — 로컬 실사용 기록 도구

- 무엇: `Study`의 별도 로컬 SQLite에 과제·방법·시작/종료 시각·monotonic 소요시간·성공 자기판정·원문 확인·수정 여부·실패 유형·메모를 기록한다. 화면과 `/api/study`의 계획/시작/종료 동선을 연결했다. 실제 사용 데이터를 생성하거나 채워 넣지 않았다.
- 왜: 경력서의 실무 효율 주장은 검색 응답 지연이 아니라 사용자가 원문 검증을 끝낸 과제 시간과 정확한 결과에 근거해야 한다. 계획 생성은 과제 순서를 섞고 과제마다 baseline/RAG의 먼저 수행할 방법을 교대 배정한다. 반복 과제의 기억 효과를 제거했다고 주장하지 않는다.
- 판정: 해결했다고 체크해도 원문 미확인·수정 필요·실패 유형이 있으면 verified_success는 false다. 원래 자기판정 플래그도 보존해 나중 검토할 수 있다. 중복 시작/완료와 잘못된 입력은 거절한다. 앱 재시작 시 진행 중 기록은 interrupted로 남기며 시간/성공을 추정하지 않는다. 남은 pending을 취소한 뒤에는 그 interrupted 행을 보존하고 같은 과제·방법의 새 pending 측정을 배정할 수 있다.
- 집계/내보내기: 완료된 자기판정만 방법별 완료 수·검증 성공률·중앙 시간을 집계한다. 같은 과제의 두 방법이 모두 검증 성공한 경우에만 baseline−RAG 시간차를 비교한다. `/api/study/export`과 화면 다운로드는 이 집계와 출처 설명만 내보내며 과제명·메모·경로·개별 trial을 포함하지 않는다.
- 검증: 10과제의 20측정과 방법 순서 균형, 12.5초의 독립 시계 주입, 원문 미확인/수정/모순 실패 유형의 성공 제외, 중복 제출 거절, 재시작 보존/중단 처리, 취소 뒤 중단 측정 재배정, 비공개 집계의 쌍별 중앙 시간차를 테스트했다. HTTP 경로에서도 과제 생성→시작→원문 미검증 종료→중복 제출 거절·익명 내보내기·남은 측정 취소를 확인했다. 실제 업무 효율이나 2주 사용 성과를 테스트 값으로 대체하지 않는다.
- 스킬 영향/한계: Ponytail에 따라 기존 SQLite/화면/요청 유틸리티를 재사용했고 Unlazy에 측정 도구 검증을 등록했다. UI 실사용 전체 동선과 최소 2주 기록은 아직 남아 있다. 집계는 자기판정이며 통계적 유의성이나 기억 효과 제거를 주장하지 않는다. 이 구현으로 FIELD 전체를 완료하지 않는다. 데이터 파일은 이미 있는 SQLite Git 제외 규칙을 따른다. 현재 1개 앱 프로세스에서 쓰는 개인 기록 도구다.

## 진행 기록 — 선택 폴더 자동 갱신

- 무엇: 기존 활성 소스/확장자/제외 규칙을 따르는 `FolderWatcher`, HTTP 시작/중지, CLI `--watch`, 화면 상태와 제어를 연결했다. 기본은 꺼짐이며 API 모드 자동 전송/비용을 시작 전에 안내한다.
- 왜/어떻게: Ponytail에 따라 OS별 이벤트 라이브러리 대신 5초 폴링을 사용한다. 수정 시각만 같게 유지된 편집도 감지하도록 해시를 비교한다. 동일 스냅샷이 최소 2초 이상 관찰된 후 기존 단일 작업 색인을 호출해 연속 변경을 합친다. 실제 기본 간격은 5초라 보통 2회 관찰이 필요하다. 검사 중 내용이 바뀌면 안정화 대기, 파싱/임베딩 중 변경은 기존 파일 보호를 재사용한다.
- 상태/복구: 진행 중 작업과 중복 시작하지 않는다. 실패는 30초 뒤 재시도하며 사라진 루트/읽기 오류는 실패 상태에 표시하고 정상 코퍼스처럼 삭제 처리하지 않는다. 완료 기준은 시작 당시 스냅샷이므로 색인 중 새 변경이 생기면 후속 작업에서 처리한다. 자동 작업을 취소하면 감시를 일시 중지해 취소 직후 다시 시작하지 않는다. 감시 중지는 진행 중 색인의 취소와 분리한다.
- 검증: 시간 주입 테스트로 연속 저장 병합/안정화, 삭제 반영, 실패 후 backoff/복구, 비활성 소스 제외, 중지 후 미실행, 해시 중 변경 대기, 완료 관찰 전 수정 미누락, 취소 후 일시 중지를 확인했다. 실제 감시 스레드 시작/종료와 HTTP 제어도 실행했다. Python 74개 테스트와 JS 요청 검증 통과. 아직 실제 브라우저에서 자동 저장→검색 반영까지의 전체 동선은 확인하지 않았으므로 P2-WATCH 전체 완료라고 표시하지 않는다.
- 한계: 폴링 전체 파일 해시는 IO 비용이 있다. 모든 활성 파일이 안정돼야 작업을 시작하므로 계속 쓰는 파일 하나가 전체 갱신을 늦출 수 있다. 긴 파일 읽기/디렉터리 열거는 즉시 강제 중단하지 않으며 중지는 해당 읽기가 끝날 때 반영될 수 있다. 실패 재시도는 감시를 중지할 때까지 계속되므로 장기 장애에서 상태를 확인해야 한다. 재시작 후 감시 켜짐 상태는 자동 복원하지 않는다.

## 진행 기록 — 제한된 임베딩 배치와 응답 검증

- 무엇: API 임베딩 요청을 최대 64개 입력·합계 64,000 UTF-8 바이트로 나누며 개별 입력은 8,000바이트까지 허용한다. 모든 입력을 먼저 검사해 마지막 입력이 잘못됐을 때 앞부분만 과금 요청하는 일을 피한다. 로컬 해시 모드는 변경하지 않았다.
- 왜/공식 확인: [OpenAI 임베딩 가이드](https://developers.openai.com/api/docs/guides/embeddings#embedding-models) 본문에서 기존 embedding 모델의 최대 입력을 확인했다. API 참조 fetch는 본문 없이 stub만 반환해 가이드로 확인을 보완했다. 위 바이트 한도는 공식 토큰 한도와 동일한 단위가 아니라 앱의 보수적인 운영 정책이다. 토큰 계산 의존성을 추가하지 않으며 긴 근거를 자동 절단하지 않는다. 제한을 넘으면 chunk 크기/질의 길이를 줄이라는 오류를 반환한다.
- 응답: 인덱스가 입력 전체를 정확히 한 번씩 포함해야 하며 중복·누락·범위 밖·비정수 인덱스를 거절한다. 빈 벡터·서로 다른 차원·문자열/boolean·NaN/Infinity를 거절하고 묶음 간 차원 변경도 확인한다. 응답 배열 순서가 달라도 index 필드 기준으로 입력 순서를 복원한다.
- 검증: 130입력의 64/64/2 분할, 한국어 11입력의 바이트 기준 10/1 분할, 전체 원문/벡터 순서, 유효하지 않은 후반 입력의 외부 호출 0회, 잘못된 응답과 묶음 간 차원 변경을 테스트했다. 70절 문서에서 첫 묶음 성공·두 번째 실패를 주입해 기존 인용 보존, 재시도 성공 뒤 교체를 확인했다. Python 66개 테스트 통과. 실제 과금 호출은 하지 않았다.
- 스킬 영향/한계: Ponytail에 따라 기존 파일 단위 트랜잭션을 그대로 이용했다. 전체 문서의 모든 묶음이 성공해야 DB를 교체하며 성공한 첫 묶음의 API 비용까지 취소되지는 않는다. 중지는 여전히 파일 경계이고 전체 문서의 벡터는 메모리에 모은다. 응답 바이트/생성 출력/전체 벽시계 제한·영구 usage 기록은 별도로 남아 있다. 본 변경만으로 전체 P1-7 완료라고 보지 않는다.

## 진행 기록 — API 재시도와 사용량 관찰

- 무엇: 임베딩과 생성 요청에 공통 `APIRequests`를 연결했다. 최대 3회 시도하며 재시도 시작/대기 예산을 45초로 제한한다. `Retry-After`의 초/HTTP 날짜를 읽고 jitter를 더한다. 헤더가 없거나 잘못되면 지수 대기를 사용한다. 대기가 남은 예산보다 길면 헤더를 무시하고 빨리 재요청하지 않고 실패를 반환한다.
- 왜/공식 근거: [OpenAI rate-limit 가이드](https://developers.openai.com/api/docs/guides/rate-limits#retrying-with-exponential-backoff)를 검색 후 본문까지 읽었다. 자체 HTTP 클라이언트는 Retry-After와 재시도 횟수/시간 제한을 지키고 quota/billing 오류를 재시도하지 않아야 한다. OpenAI Docs에 따라 기존 모델과 프롬프트는 유지했다. API 키 설정 전용 도구는 제공되지 않았고 실제 유료 호출은 하지 않았다.
- 정책: 명시적인 일시 rate-limit 코드의 429, 500/502/503/504, TimeoutError만 자동 재시도한다. 400/401, quota/billing, 원인을 모르는 429, 기타 연결/인증서 오류는 사용자 확인으로 넘긴다. timeout 뒤 재시도는 이전 요청의 미처리를 보장하지 않으므로 중복 계산/과금 가능성이 있다.
- 기록: 각 호출 객체의 최근 100회 시도를 메모리에 보관하고 `/api/status`의 `api_attempts`에 노출한다. 모델/endpoint/시각/시도 번호/상태/지연/응답의 토큰 카운터만 기록하며 질문·문서·API 키·원문 오류 본문은 저장하지 않는다. 사용량이 없으면 null, 비용은 단가 연결 전까지 null이다. `received`는 JSON 응답 수신이며 인용 품질이나 벡터 내용 검증 성공을 뜻하지 않는다.
- 검증: rate-limit 후 성공, Retry-After 최소 대기, 영구 오류/긴 헤더의 재시도 0회, 연속 실패 3회 종료, 시간 예산 소진 전 미호출, 두 실제 호출 경로 연결, 민감 본문이 없는 기록을 mock으로 검증했다. 전체 Python 62개 테스트가 통과했다. Ponytail에 따라 별도 SDK/재시도 의존성 없이 표준 라이브러리를 공유하고 Unlazy에 전용 검증을 추가했다.
- 남은 한계: 45초는 재시도 시작 예산과 소켓 timeout이며 느리게 조금씩 도착하는 응답의 전체 벽시계 시간을 강제로 끊는 보장은 아니다. 기록은 재시작 시 사라지고 영구 사용량 저널/실측 비용 보고는 아직 없다. 입력 배치 크기·응답 크기·생성 출력 제한도 후속 구현해야 하므로 P1-7 전체는 미완료다.

## 진행 기록 — 다운로드 원문 일관성

- 무엇/왜: 기존 원본 제공 경로는 해시 검사와 다운로드 읽기를 따로 수행해 그 사이 파일이 수정되면 다른 버전의 내용을 정상 응답으로 반환했다. 인용한 버전과 내려받은 파일이 같아야 한다는 P0-4의 신뢰 조건을 보강했다.
- 어떻게: 파일을 한 번 읽은 바이트의 SHA-256을 검사하고 동일 바이트를 반환한다. 달라진 내용은 409와 재색인 안내를 반환한다. Ponytail에 따라 파일 잠금이나 버전 저장 서비스를 추가하지 않고 표준 라이브러리로 두 번 읽기를 제거했다.
- 검증: 읽기 직전에 파일 내용을 바꾸는 테스트가 수정 전에는 `HTTPError not raised`로 실패하는 것을 확인했다. 수정 후에는 409를 반환한다. 읽기 직후 파일이 바뀌는 대조군에서는 검사한 기존 스냅샷만 반환하고 읽기가 1회임을 확인했다. 전체 Python 57개 테스트와 JS 요청 검증이 통과했다.
- 한계: 원본 전체를 메모리에 읽는 기존 방식은 유지한다. 아주 큰 파일의 메모리 한도나 HTTP Range 지원은 추가하지 않았다. 원문 일관성 수정만으로 PDF 페이지 이동·인용 복사의 모든 실제 브라우저 완료 조건을 충족했다고 보지 않는다. 실행 중 서버에는 재시작 후 적용된다.

## 진행 기록 — 화면 요청 대기 제한

- 무엇: 공통 화면 요청에 120초 제한을 두고 응답 헤더뿐 아니라 JSON 본문을 읽는 동안에도 취소 신호를 유지한다. HTTP 오류 문구와 JSON이 아닌 응답을 구분하고, timeout 후 기존 버튼 복구 경로로 돌아간다.
- 왜: 서버 응답을 영원히 기다리는 화면은 사용자가 실패 여부를 판단할 수 없다. 반대로 브라우저 대기 중단을 서버 작업 취소로 표현하거나 POST를 자동 재시도하면 중복 작업을 유발할 수 있다. 작업 상태 확인 후 수동 재시도를 안내한다.
- 어떻게: Ponytail에 따라 브라우저 표준 AbortController를 사용하며 새 프레임워크는 추가하지 않았다. `web/request.js`를 고정 경로로만 제공한다. 성공/실패 모두 타이머를 정리한다. Unlazy 검증 장부에 JS 실행 검사를 별도로 등록했다.
- 검증: Node의 VM에서 실제 요청 함수와 화면 run 함수를 실행해 성공 뒤 타이머 정리, 서버 오류 보존, 잘못된 JSON 안내, 헤더/본문 지연 timeout, 자동 중복 요청 없음, 버튼 재활성화와 안내 표시를 검증했다. HTTP 테스트는 실제 JS 파일의 MIME/내용과 HTML 연결을 확인한다. 이 검사는 실제 브라우저에서 120초 장애를 관찰한 것과는 구분한다.
- 한계: 브라우저 timeout은 백엔드 파싱/네트워크를 강제 종료하지 않는다. API 자체의 전체 실행 예산·배치·재시도·usage는 아직 남아 있다. 브라우저가 백그라운드 탭 타이머를 늦추면 정확한 벽시계 120초 종료를 보장하지 않는다. 이전 실행 서버는 새 정적 경로를 반영하려면 재시작해야 한다.

## 진행 기록 — 명시적 로컬 실행

- 무엇: `--mode offline`은 환경 API 키를 무시하고 색인/검색/질문 전체에 빈 API 키를 명시적으로 전달한다. `auto`는 기존 환경 기반 동작을 유지한다. 화면과 시작 로그에 실제 검색/생성 모드, 청킹 설정, 외부 전송 가능 여부를 보여준다. `--open-browser`는 실제 바인딩된 포트의 로컬 주소를 기본 브라우저에 연다.
- 왜: 회사 PC에 다른 용도로 등록한 API 키가 있다는 이유만으로 개인 문서가 전송되면 안 된다. 키 삭제 대신 실행 옵션으로 의도를 명확히 하되 기존 실행 방식은 바꾸지 않았다. offline에 별도 사용자 정의 answerer를 주입하는 우회는 명시적으로 거절한다.
- 검증: 환경 키를 설정하고 socket 연결을 차단한 상태에서 백그라운드 색인·미변경 색인·검색·인용 답변을 실제 실행했으며 연결 호출 0회를 확인했다. auto의 전송 가능 표시, 잘못된 모드 거절, 벡터 모드 변경 후 검색 거절→재색인→검색 회복, 브라우저 실패 후 서버 유지/종료 정리를 테스트했다. 전체 55개 테스트 통과. 브라우저 자동 실행은 mock 경계 테스트이며 실제 기본 브라우저 앱 실행 검증은 아니다.
- 실제 화면: 로컬 검증 서버 52243의 브라우저에서 `offline`, `local:hash-256`, `local:extractive`, `structured/900/160`, ‘외부 API 전송 없음’ 안내와 활성 폴더가 표시됨을 확인했다. 이 화면 확인을 네트워크 차단 테스트 대신 사용하지 않는다.
- 한계: 옵션은 프로세스의 RAG 호출 경로를 제어하며 OS 방화벽이나 브라우저 확장의 통신까지 차단하지 않는다. 로컬 방식은 학습된 의미 임베딩/LLM이 아니다. API batching/retry/usage와 UI 요청 timeout은 아직 남아 있어 P1-7 전체는 미완료다. 스킬 적용으로 별도 설정 서비스 없이 기존 생성자와 표준 라이브러리를 사용했고, 검증 장부에 완료 범위를 제한했다.

범위: PROJECT_IMPROVEMENTS_AND_ROADMAP.md 전체. P2도 한 가지로 축소하지 않고 모든 확장 후보를 추적한다. 조직 권한·멀티테넌시·사내 커넥터·자동 원본 수정은 원래 범위 밖이다. 기존 구현은 재검증 전 완료로 간주하지 않는다.

## 요구사항과 완료 증거

| ID | 요구사항 | 필요한 증거 | 상태 |
|---|---|---|---|
| P0-1 | 활성 소스의 검색·통계·출처 일치 | A/B 혼합 DB 및 다중 루트 비활성화 테스트 | 기존 단일 루트 구현, 다중 루트 진행 |
| P0-2 | 파일별 원자적 교체와 실패 보존 | embedding/SQL 실패·재시도 테스트 | 기존 구현 재검증 필요 |
| P0-3 | 동시 요청·손상 문서·timeout 오류 | 중복 색인/검색/손상 PDF·DOCX/API timeout, UI 오류 확인 | 단일 index job·파일 실패 보존·request deadline과 input/conflict/server 오류 JSON 분류 구현; 실제 모든 오류 유형 UI 검증은 일부 |
| P0-4 | 실제 인용 UX와 ID 기반 원본 접근 | 검색·질문 링크/복사/PDF 페이지 브라우저 검증 | 샘플 브라우저 질문→인용→강조→복사와 PDF page-fragment HTTP 검증 구현; 실제 PDF browser/OS 원본 열기 검증 일부 |
| P0-5 | 의미적 인용·coverage·다중 근거 recall | 엉뚱한 문장/거짓 주장 음성 대조군, offline 네트워크 차단 | literal 근거 recall·표시 인용 coverage·거짓 cited claim 음성 대조군과 private claim 검수 입력 구현; semantic 판단/사람 검수 결과 미완료 |
| P1-1 | 화면의 복수 폴더·활성화·필터·미리보기·저장 | 실제 범위와 preview 일치, 재시작 영속화 | 구현 및 테스트 3개, 브라우저 21→20개 비활성화 확인 |
| P1-2 | metadata/hash·버전 재색인·작업 기록·진행률·재시도·전체 색인 | 호출 수/수정 1개/버전 변경/실패 목록 테스트 | 구현 및 회귀 검증; 브라우저 최종 상태 재확인 필요 |
| P1-3 | 벡터/BM25 캐시·FTS 결정·규모별 벤치마크 | 동등 품질, p50/p95·메모리·색인시간 보고 | 캐시 구현·점수 동등성 검증·20/200/1000 문서 측정; 실제 코퍼스 확인 별도 |
| P1-4 | 한국어 변형·동의어·제목/경로·식별자 | 유형별 실패 세트 및 개선/회귀 비교 | 개발 11질의 유형별 비교·원래 평가 세트 회귀 비교; 독립 holdout은 DATA 단계 |
| P1-5 | 근거 충분성·뒤쪽 정답·주장 인용·부분 답변 | 관련 있지만 답 없음/후반 근거/부분 충족 테스트 | 후반 문장·전체 컨텍스트·구간 표시와 전후·이유·p50/p95의 좁은 literal 부분 근거 처리를 구현; 일반 의미 검증 미완료 |
| P1-6 | 대상·속성·값·적용일 기반 충돌/최신성 | 별개 지표 비충돌, 같은 결정 충돌, 명시 변경 전후 | 명시적 대상/속성 수치 비교, 원문 변경문 이력, 단일 명시 변경 기록 답변을 구현; 적용일·entity resolution·다중 기록 최신성 미완료 |
| P1-7 | 자동 브라우저·명시 offline·전송 표시·배치·재시도·timeout·usage | 실패 복구 UI 및 네트워크/모델 불일치 테스트 | 명시 offline·전송 표시·브라우저·UI timeout·제한 재시도·배치/벡터 검증·영속 content-free usage·2MB response/12k output cap 구현; 엄격 전체시간/가격 계산 미완료 |
| DATA | 가상 업무 코퍼스·다중 근거 라벨·유형·dev/holdout·검수 상태 | 문서 ID/문장/위치/사실/답변가능성 라벨 검증 | 가상 업무 4문서·6질의·문서/헤딩/문장 다중 근거 literal 검증 구현; answerability·분리 holdout·사람 검수 미완료 |
| E1 | 검색 방식 독립 비교 | 동일 설정 및 질의별 결과 | local hash embedding/동일 top-k·청킹·질의로 vector/BM25/hybrid 비교 구현; AI 개발 세트·독립 holdout 미완료 |
| E2 | 고정/구조 청킹 비교 | 원문 근거 기준 recall·긴 문서 사례 | 전략 선택·가상 업무 4문서/6질의/8근거 비교 실행; 900/160과 300/40 결과 및 실패 분석 기록, 독립 검수 아님 |
| E3 | rerank on/off 독립 비교 | 품질/지연/실측 비용 | 동일 hybrid 조건의 규칙 rerank on/off·지연·오프라인 비용 비교 구현; 학습 reranker/실측 비용·holdout 미완료 |
| E4 | 생성 정책별 인용·보류 비교 | 주장 검수/거짓 답변/coverage 및 정책 선택 | literal completeness permissive/partial 정책 격리 비교 구현; 의미적 claim 검수·생성 정책 비교·holdout 미완료 |
| META | 모델·코드·데이터·청킹·topk·프롬프트·시간·usage·가격 기준 | 재현 보고서 검증, 미측정 비용 null | 일부 구현 |
| FIELD | 2주 개인 사용 및 10~15개 비교 과제 | 로컬 기록 도구·순서 통제·성공률/시간·집계, 실제 사용자 기록 | 계획/순서 배정/타이머/판정/영속화·중단 재배정·비공개 쌍별 집계/내보내기 구현; 실제 2주 측정은 미완료 |
| FAILURES | 5개 이상 실패 분석·경력서 증거 | 재현 질의/근거/원인/변경/전후 결과 및 한계 | 재현 실패 후보 5개 큐 구현; 원인/변경/전후 결과와 사람 검수 미완료 |
| P2-HISTORY | 결정 이력 비교 | 날짜·변경 전후·출처·추측 구분 | 원문 명시 변경문·날짜순·출처·null 추측 구분 구현; 다문서 동일 결정 묶음/coverage 미완료 |
| P2-BUNDLE | 관련 문서 묶음 | 다중 문서 coverage 전후 비교 | 문서별 최고 근거 다양화·UI 구현; 실제 multi-document coverage 전후 비교 미완료 |
| P2-LOCAL | 학습된 로컬 임베딩 | 실모델 설치 크기·메모리·품질·속도·offline 확인 | 선택형 local-files-only sentence-transformers 경로와 모델 SHA/바이트·Python peak·지연 평가 명령 구현; 실제 모델 자산/독립 holdout 측정 미완료 |
| P2-RERANK | 학습된 reranker | 실모델 품질/추가 지연/비용 비교 | 선택형 local-files-only cross-encoder 경로와 rerank on/off 평가 명령 구현; 실제 모델 자산/독립 holdout 측정 미완료 |
| P2-FORMAT | PPTX/XLSX/HWP 등 위치 보존 파싱 | 슬라이드/시트·셀/절 인용 fixture | PPTX/XLSX/HWPX 위치 보존 파싱·fixture 구현; 구형 HWP 및 표/이미지 의미 검수 미완료 |
| P2-OCR | 스캔 문서 OCR | 페이지 위치·오류 표시·품질 검수 | 선택형 local Tesseract 이미지 PDF OCR, `page N · OCR` 위치·페이지별 오류·백그라운드 전달 구현; 실제 OCR 런타임/스캔 문서 품질 검수 미완료 |
| P2-WATCH | 폴더 자동 갱신 | 저장중 보호·이벤트 합치기·재시도·중지 | 폴링/안정화/재시도/중지·UI/HTTP/CLI 구현 및 테스트; 실제 브라우저 편집→반영 동선 미확인 |
| P2-REVIEW | 개인 평가 UI | 근거·답변·판정·실험 차이 화면 및 저장 | 답변·표시 근거·사람 판정/메모와 로컬 실험별 판정 집계 구현; 라벨 가이드·실제 검수 데이터 미완료 |
| P2-RELATIONS | 프로젝트→결정→문서 관계 | 관계 탐색과 multi-document recall 비교 | 수동 관계 저장·범위 검증·관계 탐색과 AI 개발 fixture의 search/relation coverage 비교 구현; 실제 관계 질의·자동 관계 추출 미완료 |
| DOCS | 무엇/왜/어떻게/검증/한계 | 구현 기록·README·실행 재현 및 전체 완료 감사 | 진행 |

## 실행 순서

1. 소스 설정과 일관된 접근 범위 → 버전/작업 상태/비동기 색인.
2. 검색 계산 재사용·한국어/식별자·근거 판단·충돌 처리.
3. 평가 코퍼스와 E1~E4·실패 분석·개인 기록/검수 화면.
4. 결정 이력·관련 문서·관계·추가 형식/OCR·로컬 학습 모델·자동 갱신.
5. 전체 회귀/벤치마크/실제 브라우저 및 요구사항별 완료 감사.

각 단계는 전체 목표의 중간 진행일 뿐이다. 실제 사용기간·사람 검수·실모델 품질 개선을 구현 코드나 mock 테스트로 대체하지 않는다. 공개 GitHub push는 이번 구현 요청에 포함된 것으로 추정하지 않으며 별도 요청 전 로컬 변경으로 유지한다.

## 진행 기록 — 소스 설정

- 무엇: `rag/sources.py`에 폴더 목록·필터·미리보기·로컬 영속화, 서비스/HTTP/UI에 등록과 비활성화 동선을 연결했다.
- 왜: 미리보기에서는 제외되지만 검색에는 남는 불일치를 방지하기 위해 하나의 허용 판정을 검색·통계·출처·원본 접근에 적용했다.
- 어떻게: SQLite 조회에 공통 경로 판정을 적용하고, 색인 후보에도 같은 필터를 사용한다. 설정 변경은 DB 접근 잠금 안에서 적용한다. 비활성 파일을 원본에서 삭제하지 않는다. 설정 파일에는 개인 경로가 있어 Git에서 제외한다.
- 검증: 두 루트의 문서 검색, 비활성화 후 즉시 검색/통계/출처/다운로드 제외, 설정 재시작 보존, 포함·제외/확장자/기본 제외, HTTP 미리보기 및 출처 404를 테스트했다. 기존 포함 총 22개 테스트 통과. 브라우저에서 sample+docs 등록 후 21개 문서, docs 비활성화 후 20개 문서로 즉시 변경됨을 확인했다.
- 한계: SQL 경로 판정은 아직 행별 수행된다(P1-3 캐시 최적화 예정). 색인 중에는 다른 DB 요청이 기다린다(P1-2 작업 상태 개선 예정). 저장된 활성 폴더가 사라진 상태의 시작 복구도 후속 검토 대상이다. 전체 목표는 미완료이며 다음 작업은 버전 기반 증분 색인과 작업 상태/재시도다.

## 진행 기록 — 증분 색인과 작업 상태

- 무엇: 엄격/메타데이터 확인, 파서·청킹 버전 재색인, 저장 중 파일 보호, 파일별 처리 상태, 백그라운드 작업·진행률·중지·실패 재시도·전체 재색인·마지막 성공 시각·중단된 작업 복구를 구현했다.
- 왜: 개인 문서 폴더가 커져도 검색을 계속 사용해야 하며, 실패/중지/아무 변경 없음과 실제 전체 성공을 구분해야 한다. 재시도에서 정상 문서까지 삭제되는 일을 방지한다.
- 어떻게: 색인 작업만 별도 SQLite 연결을 소유하고 WAL로 읽기를 허용한다. 작업 상태는 별도 로컬 JSON, 파일별 상태는 DB에 남긴다. 파일 교체 직전에 메타데이터·해시를 다시 확인한다. 파이프라인 버전은 문서/인용 ID에도 포함해 이전 인용이 다른 청크를 가리키지 않게 했다. 저장된 폴더가 일시적으로 사라져도 앱을 열어 비활성화할 수 있다.
- 검증: `tests/test_index_jobs.py`는 미변경 호출 0회, 같은 크기·mtime의 내용 변경, 버전 변경·전체 재색인, 저장 중 변경의 이전 근거 보존, 지연 임베딩 중 검색·상태·중복 거절·중지·재개, 손상 DOCX/PDF·timeout, 실패만 재시도, 성공 시각/중단 작업 재시작, 사라진 루트 복구를 확인한다. HTTP start/job/full/retry/cancel 경로도 테스트한다.
- 브라우저: 8003 화면의 작업 제어 UI와 시작 버튼을 확인하고 실행했다. 이후 브라우저 탭이 세션에서 사라져 최종 화면 검증은 중단됐다. HTTP 조회에서는 20/20, 갱신 20, 실패 0, succeeded를 확인했다. 이 HTTP 결과를 실제 화면의 최종 검증으로 대체하지 않는다.
- 한계: 중지는 진행 중 파싱/네트워크 요청을 강제 끊지 않는다. API의 배치·재시도·요청시간 제어는 P1-7에서 추가한다. 파서/청킹 코드 변경 시 개발자가 `pipeline_version`을 함께 바꿔야 한다. 작업 기록은 최근 작업과 마지막 성공 시각이며 전체 이력 타임라인은 아직 없다. 다음은 P1-3 검색 캐시와 벤치마크 및 P1-4 한국어 실패 세트다.

## 진행 기록 — 검색 캐시와 한국어 개발 실험

- 무엇: BM25 문서 통계와 벡터 JSON을 색인 revision별로 재사용한다. keyword 모드는 벡터 파싱·query embedding을 하지 않는다. 제목/경로 점수와 한국어 bigram, 명시적 RRF/RTO 한영 대응을 보조 검색에 추가했다. `lexical="legacy"`로 기존 검색 기준선을 비교할 수 있다.
- 왜: 매 질문마다 동일한 코퍼스를 다시 토큰화·JSON 파싱하는 비용을 줄이되, 성능 최적화와 검색 규칙 변경의 효과를 분리해서 측정해야 한다.
- 어떻게: SQLite trigger가 청크 변경 및 검색 관련 문서 메타데이터 변경 시 revision을 갱신한다. 캐시 키에 revision·source_root·활성 필터를 포함해 다른 연결의 색인과 폴더 비활성화 후 갱신된다. 사용하지 않던 FTS 중복 테이블은 제거했다. 원본 문서나 유일한 청크 데이터는 삭제하지 않으며 재색인으로 파생 저장소를 다시 만들 수 있다. FTS 조건부 검증은 조용히 건너뛰지 않고 SQL 실패 시 revision rollback 검증으로 교체했다.
- 검증: BM25는 기존 수식을 독립 계산해 대조했다. cached/uncached 순위와 점수 일치, 외부 writer의 수정·삭제 후 갱신, 활성 범위 변경, warm keyword의 JSON/embedding 미호출을 확인했다. `benchmark_search.py`는 20/200/1000 합성 문서에서 keyword/hybrid의 p50/p95·첫 질의 지연·Python peak allocations·색인 시간을 기록하고, 원래 평가 질의의 cached/uncached 동등성도 검사한다.
- 한국어 개발 데이터: `data/eval/retrieval_cases.json`은 AI 초안 10문서·11질의이며 사람이 검수한 holdout이 아니다. 한국어 4·용어 대응 3·정확 코드 2·파일명 1·경로 1을 분리했다. `복구시간목표`가 처음에는 부분 일치 문서를 1위로 고른 실패를 확인하고 명시적 용어 대응을 강화했다. 수정 뒤 11개 모두 정답 문서가 1위였고 정확 코드 2개도 유지됐다.
- 평가 해석: 원래 47개 답변 가능 질의에서 legacy→enhanced 문서 Hit@5는 BM25 45/47→47/47, hybrid(no rerank) 44/47→45/47, hybrid+rerank 45/47→46/47이었다. vector-only는 34/47로 동일하다. 이는 합성 문서 검색의 결과이며 답변 내용의 정확도나 실제 업무 효율이 아니다. 최신 실행 수치는 `results/search_benchmark.json`을 확인한다.
- 한계: 캐시는 전수 벡터 순회를 없애지 않는다. 메모리는 tracemalloc으로 Python 할당만 측정하며 계측 자체의 시간 오버헤드가 있다. 비캐시 실행이 먼저라 순서/캐시 효과가 남고 첫 질의는 cold다. 한국어 기능은 보조 규칙이며 새 용어·복잡한 문법 일반화를 보장하지 않는다. 실제 corpus와 독립 holdout의 검증은 여전히 필요하다.

## 진행 기록 — 근거 문장·인용 위치·충돌 검사

- 무엇: 화면 발췌와 모델 컨텍스트를 분리하고, 질문 관련 문장을 청크 전체에서 선택한다. 발췌에 문자 offset을 기록해 브라우저에서 원문 구간을 강조한다. 모델 출력의 인용 오류/timeout 시 동일한 sources 목록으로 발췌 답변을 생성한다. 인용 없는 문장이 섞인 출력도 형식 오류로 처리한다.
- 왜: 화면에 앞 500자만 보여주는 제한이 모델 근거까지 잘라내면 뒤쪽 정답을 놓친다. 파일명·메타데이터 검색 점수만으로 본문이 질문을 뒷받침한다고 보지 않아야 한다. API 오류 때도 인용 번호가 실제 출처와 일치해야 한다.
- 어떻게: `select_evidence_sentence`는 기존 한국어/용어 검색 특징을 재사용한다. 날짜를 요구하는 일부 질문에는 명시 날짜/요일이 있어야 후보로 인정한다(변경 이유 질문에는 이 제한을 적용하지 않음). 단순 수치 차이 경고를 대상+명시 속성+단일 값 비교로 교체해 p50/p95와 서로 다른 프로젝트를 분리한다. 값이 여러 개인 변경/범위 문장은 이 규칙으로 단정하지 않는다.
- API 확인: OpenAI Docs를 적용했다. 생성 API 참조 본문은 크기/timeout으로 직접 열리지 않아 [공식 Text generation 가이드](https://developers.openai.com/api/docs/guides/text)를 실제로 읽고 `instructions`/`input` 분리와 output message 순회를 확인했다. 모델은 변경하지 않았고 실제 유료 API는 호출하지 않았다. 테스트는 모의 응답이다. 문서 내용은 명령이 아닌 비신뢰 데이터로 처리하도록 지시를 추가했다.
- 검증: 500자 뒤 문장이 선택되는지, 전체 컨텍스트가 모델 호출 경계에 전달되는지, timeout/잘못된 인용 번호 뒤에도 실제 발췌와 번호가 일치하는지, 인용 없는 두 번째 문장을 거절하는지, 메타데이터만 일치하거나 날짜가 없는 관련 문서를 보류하는지 검사했다. 같은 대상의 일정 상충은 경고하고 다른 대상/다른 지표는 경고하지 않는 대조군을 추가했다. HTTP 테스트는 HTML escaping과 offset 범위를 검사한다.
- 브라우저: 8004 로컬 모드에서 20/20 색인 완료 상태, RRF 질문의 발췌, `start/end#quote` 링크와 해당 원문 구간으로 이동한 화면을 확인했다. 이전 단계에서 탭 소실로 미확인했던 작업 완료 상태도 이번에 확인했다.
- 남은 한계: 이 선택기는 학습된 entailment 판단이 아니다. 숫자/속성 파싱은 일부 패턴만 다루고 주제·적용일·변경 전후를 일반적으로 이해하지 않는다. 모델 주장이 실제 근거를 따르는지, 부분 답변, 근거 없는 사실에 유효 번호를 붙인 출력의 의미적 거절은 아직 완료하지 않았다. 이 때문에 P0-5/P1-5/P1-6 전체를 완료로 올리지 않는다. 향후 평가에는 사람이 검토한 주장/근거 라벨과 적용일을 포함해야 한다.
- 평가 관찰: 현재 정답 파일 인용 비율은 vector-only 42.2%, BM25 86.7%, hybrid 62.2%, hybrid+rerank 73.3%다. 초기 vector-only 53.2%보다 낮아졌으므로 모든 검색 구성에서 답변 품질이 개선됐다는 결론은 내리지 않는다. 이 값도 파일명 일치 proxy일 뿐이고, 의미적 정확도는 null로 남긴다. 회귀 원인과 정책별 대조는 E4에서 추가 분석해야 한다.

## 진행 기록 — 명시적 다중 사실 질문의 부분 근거 처리

- 무엇: 질문에 `전후`, 변경 `이유`, `p50`/`p95`가 명시됐을 때, 검색된 원문에 해당하는 리터럴 근거가 빠지면 `partial` 상태로 표시하고 생성 API를 호출하지 않는다.
- 왜: 한두 개의 관련 문장만으로 변경 전후·이유·두 성능 지표를 모두 찾은 것처럼 보이는 답변을 줄이기 위해서다.
- 어떻게: 전후에는 두 개 이상의 날짜/수치, 이유에는 명시적 이유 표지, 각 지표에는 정확한 `p50`/`p95` 표기를 요구한다. 충족하지 못하면 발췌와 빠진 항목을 함께 보이며 원문 확인을 요구한다.
- 검증: 이유가 빠진 변경 전후 질문, p95가 빠진 p50/p95 질문은 `partial`이며, 명시적 `because`가 있는 변경 문장은 기존 extractive fallback으로 처리되는 단위 테스트를 추가했다.
- 한계: 이는 범용 사실 분해·함의 판정이 아니라 의도적으로 좁은 리터럴 규칙이다. 복잡한 비교/기간/동의어의 완전성은 P0-5의 사람 검수 기반 claim 라벨과 E4에서 검증해야 한다.

## 진행 기록 — API 응답 크기 제한

- 무엇: 외부 embedding/answer 요청이 반환하는 JSON 본문을 2MB까지만 읽고 초과 응답은 오류로 남긴다.
- 왜: 개인 문서 내용이나 비정상 응답이 무제한으로 메모리에 축적되어 UI가 오래 멈추는 경로를 줄이기 위해서다.
- 어떻게: 기존 3회/45초 재시도 경계를 유지하고, 두 API 경로가 공유하는 `APIRequests`에서 `read(2MB + 1)` 후 상한을 검사한다. 기록에는 내용 없이 실패 상태와 지연만 남긴다.
- 검증: 초과 크기의 모의 응답이 상한 크기만 읽도록 요청되고 `ValueError` 및 content-free 실패 기록으로 처리되는 단위 테스트를 추가했다.
- 한계: 네트워크 스택의 개별 socket read를 강제로 중단하는 별도 취소 메커니즘은 아니다. 실시간 스트리밍, 모델별 가격표/정확 비용 산출은 구현하지 않았으므로 P1-7 전체 완료로 표시하지 않는다.

## 진행 기록 — 생성 답변 표시 상한

- 무엇: Responses API에서 추출한 생성 답변이 12,000자를 넘으면 화면에 표시하지 않고, 같은 source 목록의 extractive fallback으로 전환한다.
- 왜: API JSON 전체 크기가 제한돼도 화면에 지나치게 긴 생성 답변이 들어오면 원문 검증 흐름을 방해할 수 있기 때문이다.
- 검증: 12,001자 모의 model output이 fallback과 기존 source를 유지하고 오류에 character-limit 원인을 남기는 단위 테스트를 추가했다.
- 한계: 이는 최종 텍스트 표시 제한이며 스트리밍 중단/토큰별 비용 제한은 아니다.

## 진행 기록 — 재현 가능한 실패 분석 큐

- 무엇: `scripts/analyze_failures.py`가 평가 report와 질의 라벨에서 검색/보류 실패 후보 5개를 질문, 기대 문서, 설정, top result와 함께 추출한다.
- 왜: 결과 파일 전체를 수동으로 읽지 않고, 실패 원인·수정·전후 수치를 사람이 같은 조건에서 조사할 시작점을 만들기 위해서다.
- 검증: 현재 `results/evaluation.json`에서 5개를 추출하고, root cause/change/before-after가 모두 `null`, `needs_human_investigation` 상태로 남는 것을 테스트했다.
- 한계: 후보 선정은 AI 작성 개발 평가의 observed failure일 뿐이다. 원인 분석·수정·전후 개선과 사람 검수는 아직 수행하지 않았으므로 FAILURES/E4 전체 완료가 아니다.

## 진행 기록 — E1/E3 오프라인 검색 비교

- 무엇: `scripts/run_eval.py`가 같은 local hash embedding, structured 900/160 청킹, top-k=5, 50개 질의에서 vector-only, BM25, hybrid(re-rank off/on)를 함께 실행한다.
- 결과: 최신 실행의 document Hit@5는 vector 0.723, BM25 1.000, hybrid off 0.957, hybrid on 0.979였다. hybrid의 규칙 rerank는 off 대비 document Hit@5를 0.021 올렸고 p50 전체 지연은 2.130ms→2.165ms였다. 오프라인 API 비용은 0이며 local compute/human review 비용은 null이다.
- 해석: 이 숫자는 합성 개발 코퍼스에서의 파일 발견 proxy다. 의미적 인용 precision/grounded answer rate는 null이고, holdout·실제 문서·학습 reranker 성능이나 업무 시간 개선을 뜻하지 않는다.

## 진행 기록 — E4 literal 부분 근거 정책 비교

- 무엇: `scripts/evaluate_answer_policy.py`가 동일한 추출형 답변에서 permissive 정책과 명시 requirement를 `partial`로 처리하는 정책을 비교한다.
- 결과: 문서 범위를 case별로 고정한 AI 작성 fixture의 불완전 2건에서 permissive의 partial rate는 0.0, required-literal partial rate는 1.0이었다. 완전 변경 case는 required 정책에서 partial이 아니었다.
- 해석: 이는 전후·이유·p50/p95라는 좁은 literal 규칙이 의도한 상태 전환을 수행한다는 증거일 뿐이다. 검색·모델 생성·패러프레이즈/함의·사람 claim 검수나 실제 잘못된 답변 감소율은 측정하지 않는다.

## 진행 기록 — 브라우저 원문 확인 경로

- 무엇: 로컬 offline 서버에서 샘플 문서의 질문 결과가 근거 카드, 원문 위치 링크, 강조된 원문, 인용 복사까지 이어지는 브라우저 흐름을 다시 확인했다.
- 왜: HTTP 응답의 문자열 검사만으로는 사용자가 실제로 인용 문장에 도달하고 복사할 수 있는지 증명할 수 없기 때문이다.
- 검증: `What does RRF combine?` 질문에서 `02_hybrid_retrieval` 발췌가 표시되고, `근거 위치 확인` 링크가 새 원문 뷰어의 강조 문장으로 열렸으며, `인용을 복사했습니다` UI 알림을 확인했다. 실행은 `data/sample`, offline, 127.0.0.1:8020에서만 수행했다.
- 한계: PDF 페이지 이동, OS 원본 파일 열기, 회사 문서/권한 환경은 이 샘플 브라우저 검증에 포함되지 않는다. 따라서 P0-4 전체 완료로 표시하지 않는다.

## 진행 기록 — 단일 명시 변경 기록 답변

- 무엇: 변경 질의에서 검색된 결과에 명시적 변경 문장이 정확히 하나일 때, 그 문장의 변경 전·후·이유를 동일 인용 번호로 구조화해 표시한다.
- 왜: 검색 결과의 날짜나 파일 수정 시각을 조합해 최신 결정을 추정하는 대신, 실제 변경 문서의 리터럴 기록을 사용하기 위해서다.
- 어떻게: 결정 이력 파서가 이미 추출하는 명시 필드만 사용하며, 이벤트가 둘 이상이거나 같은 결정을 가리킨다는 근거가 없으면 기존 발췌/이력 확인 흐름을 유지한다. 이 경로는 생성 API를 호출하지 않는다.
- 검증: `2026-04-10 → 2026-04-17 changed because ...` 문장이 전·후·이유와 원문 인용으로 표시되고, 최신성 단정 금지 문구가 포함되는 단위 테스트를 추가했다.
- 한계: 이것은 단일 리터럴 기록의 표시 기능이며, 프로젝트명 해석·여러 변경 기록 통합·일반 최신성 판정은 하지 않는다. 따라서 P1-6 전체 완료가 아니다.

## 진행 기록 — 명시 관계의 다중 문서 coverage 비교

- 무엇: `scripts/evaluate_relations.py`가 상위 5개 hybrid 검색과 사용자가 직접 등록한 프로젝트·결정·문서 지도 각각에서 gold 문서가 얼마나 포함되는지 따로 기록한다.
- 왜: 관계 기능이 단순히 화면에 존재하는 것과, 다중 문서 질문에서 필요한 문서를 빠짐없이 보여 주는 것은 다른 문제이기 때문이다.
- 검증: AI 작성 1개 개발 case에서 search coverage는 0.667, explicit relation coverage는 1.0이었다. 네트워크를 차단하고 API 요청 0건, 비활성/미색인 문서 0건을 확인했다.
- 한계: 관계 지도와 라벨 모두 fixture에 수동으로 적은 값이며, 자동 추출·entity resolution·실제 업무 improvement가 아니다. 사람 검수 holdout과 실제 관계 질의가 있어야 P2-RELATIONS 전체를 완료할 수 있다.
## 진행 기록 — 청킹 전략 선택과 변경 감지

- 무엇: 고정 길이(`fixed`)와 구조 기반(`structured`)을 실행 옵션으로 선택하고, 크기/겹침을 검증한다. 백그라운드 writer와 상태 API도 같은 설정을 사용한다.
- 왜: E2 실험에서 이름만 다른 동일 알고리즘을 비교하지 않도록 실제 경계 처리를 분리했다. 설정이 달라졌는데 예전 색인이 재사용되거나 과거 인용이 다른 문단을 가리키는 문제도 막아야 한다.
- 어떻게: fixed는 절을 연결한 본문에 문자 창을 적용하고 접촉한 절 위치를 보존한다. structured는 기존 절별 문단 처리를 유지한다. 전략·크기·겹침을 처리 버전과 문서 ID에 포함한다. 평가 보고서에는 실제 인덱스 설정을 기록한다.
- 검증: 청킹 전용 5개 테스트로 절 경계/겹침/내용 보존/잘못된 설정/변경 후 재색인/이전 인용 만료/백그라운드 전달을 확인했다. 전체 46개 테스트가 통과했다.
- 남은 일: E2의 동일 원문 근거 recall 비교와 긴 업무 문서 실패 분석은 아직 미실행이다. 기능 선택 가능성과 품질 우열은 별개이므로 E2 전체 완료로 표시하지 않는다. 재색인은 사용자가 시작하며 실행 설정은 CLI에 지정한다.

## 진행 기록 — E2 원문 근거 비교 실험

- 무엇: `scripts/compare_chunking.py`와 `data/eval/chunking_cases.json`으로 동일 원문/질의/검색 설정에서 fixed와 structured를 비교한다. 가상 회의록·일정 변경·런북·스펙 4문서는 각각 900자보다 길며, 질의 6개에 원문 문장/파일/헤딩으로 표시한 필수 근거 8개가 있다. 변경 전후 질문은 여러 문서의 3개 근거를 모두 요구한다.
- 왜: 정답 파일만 검색돼도 성공이라고 하면 엉뚱한 문단을 찾거나 문장이 잘린 경우를 놓친다. 청크 ID는 전략에 따라 달라지므로 정답은 원문 위치에 연결하고, 잘못된 위치/없는 문장 라벨을 실행 전에 거절한다.
- 측정: 상위 5개 중 같은 파일의 완전한 근거 문자열을 포함한 청크가 있으면 해당 근거를 찾은 것으로 센다. 전체 필수 근거에 대한 micro recall과 모든 근거를 찾은 질의 비율을 따로 기록한다. 검색 순위에서 빠진 경우와 어느 청크에도 완전한 문장이 없는 경계 분할 실패를 구분한다. 생성은 실행하지 않으므로 답변 정확도나 의미적 인용 지표가 아니다.
- 실행 결과: 기본 900자/160자 겹침에서 두 전략 모두 8/8 근거를 찾았다(fixed 8청크, structured 20청크). 300자/40자 겹침에서는 fixed 6/8, structured 8/8이었다(fixed 20청크, structured 28청크). 기본값에서 우열은 확인되지 않았고 작은 청크의 이 개발 세트에서만 차이가 관찰됐다. 기본 structured는 유지하지만 이 실험만으로 일반적 품질 우위를 주장하지 않는다.
- 실패 분석: 300/40 fixed의 `schedule`은 변경 후와 이유는 찾았지만 초기 일정이 상위 5개에서 빠졌다(원문 근거는 청크에 남아 있어 ranking 실패). `error`는 ERR_LEDGER_042 조치 문장이 청크 경계에서 나뉘어 완전한 문장 조건을 충족하지 못했다. 이 두 실패를 사후 데이터 수정으로 지우지 않고 결과에 보존했다.
- 재현: `python scripts/compare_chunking.py` 및 `python scripts/compare_chunking.py --chunk-size 300 --chunk-overlap 40 --output results/chunking_comparison_300.json`. 보고서에 원문/fixture/코드 해시, 실제 처리 버전, 검색 설정, 질의별 반환 문단, 지연/색인 시간을 남긴다. API 키를 무시하고 socket 연결을 차단한다. API 호출/비용은 0, 로컬 계산 비용은 미측정이다.
- 검증: 잘못된 파일·같은 파일의 엉뚱한 문장은 false, 정확 문장은 true라는 대조 테스트를 포함한다. 없는 근거 라벨 거절, 두 전략 실행, API 키 무시, 집계 재계산, 작은 청크 실패 기록까지 4개 테스트로 검증했다. 전체 50개 테스트 통과.
- 한계/경력서: AI 작성 개발 데이터이며 사람 검수나 독립 holdout이 아니다. 긴 PDF 표/실무 대규모 문서 검증은 아니다. 문장이 여러 청크에 걸쳐 복원 가능해도 이 단일 청크 지표는 실패로 센다. 작은 표본/고정 실행 순서/첫 질의 캐시 효과 때문에 지연을 일반화하지 않는다. 경력서에는 ‘원문 근거 기준 청킹 대조 실험과 경계·순위 실패 분석 구현’으로 쓸 수 있지만 실제 업무 시간 절감으로 쓰면 안 된다. DATA·E4·FIELD 전체 요구는 여전히 남아 있다.
